---
phase: 02-rename-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/fingerprint.ts
  - src/lib/renameEngine.ts
  - tests/fingerprint.test.ts
  - tests/renameEngine.test.ts
autonomous: true
requirements:
  - PATT-03
  - CORE-04

must_haves:
  truths:
    - "computeFingerprint('invoice-jan-2024.pdf') returns 'invoice.pdf'"
    - "computeFingerprint('report-2024-01-31.pdf') returns 'report.pdf'"
    - "computeFingerprint('screenshot-abc1234567890.png') returns 'screenshot.png'"
    - "computeFingerprint('file.pdf') and computeFingerprint('file-001.pdf') return the same key"
    - "applyTemplate('{tag}-{date}-{index}', 'invoice', 3) returns 'invoice-{YYYY-MM-DD}-3'"
    - "applyTemplate with all three slots replaced, extension appended by caller"
  artifacts:
    - path: "src/lib/fingerprint.ts"
      provides: "computeFingerprint(filename: string): string — exported pure function"
      exports: ["computeFingerprint"]
    - path: "src/lib/renameEngine.ts"
      provides: "applyTemplate(renameFormat, tag, matchCount) → string — exported pure function"
      exports: ["applyTemplate"]
    - path: "tests/fingerprint.test.ts"
      provides: "TDD unit tests for computeFingerprint — must pass before implementation ships"
      contains: "computeFingerprint"
    - path: "tests/renameEngine.test.ts"
      provides: "TDD unit tests for applyTemplate — must pass before implementation ships"
      contains: "applyTemplate"
  key_links:
    - from: "entrypoints/background.ts"
      to: "src/lib/fingerprint.ts"
      via: "import { computeFingerprint } from '../src/lib/fingerprint'"
      pattern: "computeFingerprint"
    - from: "entrypoints/background.ts"
      to: "src/lib/renameEngine.ts"
      via: "import { applyTemplate } from '../src/lib/renameEngine'"
      pattern: "applyTemplate"
---

<objective>
Build and TDD-verify the two pure-function modules that the rename engine depends on:
`computeFingerprint` (stem normalization → cache key) and `applyTemplate` (renameFormat template → final filename stem).

Purpose: These are the algorithmic core of PATT-03 (local cache hit, zero network) and CORE-04 (apply learned rule). Both must be unit-tested in isolation before they are wired into background.ts in Wave 2.

Output: src/lib/fingerprint.ts, src/lib/renameEngine.ts, tests/fingerprint.test.ts, tests/renameEngine.test.ts — all tests green.
</objective>

<execution_context>
@$HOME/Documents/Projects - Claude Main Folder/CLI/get-shit-done/workflows/execute-plan.md
@$HOME/Documents/Projects - Claude Main Folder/CLI/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rename-engine/02-CONTEXT.md
@.planning/phases/02-rename-engine/02-RESEARCH.md

<interfaces>
<!-- Contracts established by Phase 1 (read entrypoints/background.ts before touching it) -->

From entrypoints/background.ts (existing, must not be modified in this plan):
```typescript
export const storageRules = storage.defineItem<
  Record<string, { tag: string; renameFormat: string; matchCount: number }>
>('local:rules', { fallback: {} });
```

Target signatures for the new modules:

src/lib/fingerprint.ts:
```typescript
export function computeFingerprint(filename: string): string;
// Splits filename at last '.', strips noise from stem per D-02, returns '{keywords}.{ext}'
// Edge case: no extension → returns cleaned stem only
// Edge case: all tokens stripped → returns ext only (no leading dot)
```

src/lib/renameEngine.ts:
```typescript
export function applyTemplate(
  renameFormat: string,
  tag: string,
  matchCount: number
): string;
// Replaces {tag}, {date} (YYYY-MM-DD), {index} (matchCount as string)
// Returns the rendered stem — caller appends the file extension
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: TDD fingerprint.ts — RED then GREEN</name>
  <files>tests/fingerprint.test.ts, src/lib/fingerprint.ts</files>

  <read_first>
    - .planning/phases/02-rename-engine/02-CONTEXT.md — D-01, D-02, D-03 (what to strip, key format)
    - .planning/phases/02-rename-engine/02-RESEARCH.md — Pattern 3 (STRIP_PATTERNS array, computeFingerprint implementation)
    - vitest.config.ts — confirm WxtVitest plugin; no additional setup needed for pure functions
    - tests/suggest-guard.test.ts — import style and fakeBrowser usage pattern (pure functions need neither)
  </read_first>

  <behavior>
    RED phase — write all tests first, run `npm test -- --reporter=verbose fingerprint`, confirm they fail:

    - computeFingerprint('invoice-jan-2024.pdf') === 'invoice.pdf'
    - computeFingerprint('report-2024-01-31.pdf') === 'report.pdf'
    - computeFingerprint('screenshot-abc1234567890.png') === 'screenshot.png'  (UUID stripped)
    - computeFingerprint('file-001.pdf') === computeFingerprint('file.pdf')  (numeric suffix stripped → same key)
    - computeFingerprint('receipt_2024_03_15.pdf') === 'receipt.pdf'
    - computeFingerprint('document v3.docx') === 'document.docx'
    - computeFingerprint('IMG_20240101_123456.jpg') === 'img.jpg'  (or 'img.jpg' — all-date stem)
    - computeFingerprint('noextension') === 'noextension'  (no dot → return stem)
    - computeFingerprint('2024-01-01.pdf') === 'pdf'  (stem fully stripped → return ext only, no leading dot)

    GREEN phase — implement computeFingerprint using STRIP_PATTERNS from RESEARCH.md Pattern 3.
    Run `npm test -- --reporter=verbose fingerprint` — all tests must pass before committing.
  </behavior>

  <action>
    RED commit message: "test(02-01): add failing fingerprint tests"
    GREEN commit message: "feat(02-01): implement computeFingerprint"

    Implementation details (from RESEARCH.md Pattern 3 — do not deviate without noting why):
    - STRIP_PATTERNS array applied in order: ISO dates, US dates, month names, UUIDs (hex 8+ chars), trailing numeric suffixes, standalone numbers.
    - After stripping: replace non-alpha chars with space, split on whitespace, filter empty, join with '-'.
    - Return '{keywords}.{ext}' or '{ext}' (if keywords empty) or '{keywords}' (if no extension).
    - Function signature: export function computeFingerprint(filename: string): string
    - File location: src/lib/fingerprint.ts (create src/lib/ directory)
    - No imports needed — pure string manipulation, no chrome APIs, no @wxt-dev/storage.
  </action>

  <verify>
    <automated>npm test -- --reporter=verbose fingerprint</automated>
  </verify>

  <acceptance_criteria>
    - tests/fingerprint.test.ts exists and contains at least 9 test cases covering the behaviors above.
    - src/lib/fingerprint.ts exports `computeFingerprint` as a named export.
    - `npm test -- fingerprint` exits 0 with all tests passing.
    - No imports of chrome.*, @wxt-dev/storage, or any external library in fingerprint.ts (pure function).
    - computeFingerprint('invoice-jan-2024.pdf') returns exactly 'invoice.pdf' (verified by test output).
    - computeFingerprint('file-001.pdf') === computeFingerprint('file.pdf') (same key for numeric variant).
  </acceptance_criteria>

  <done>Both RED and GREEN commits exist. All fingerprint tests pass.</done>
</task>

<task type="tdd">
  <name>Task 2: TDD renameEngine.ts — RED then GREEN</name>
  <files>tests/renameEngine.test.ts, src/lib/renameEngine.ts</files>

  <read_first>
    - .planning/phases/02-rename-engine/02-CONTEXT.md — D-07, D-08, D-09 (template slots, apply-time values, extension handled by caller)
    - .planning/phases/02-rename-engine/02-RESEARCH.md — Pattern 6 (applyTemplate implementation)
    - src/lib/fingerprint.ts — confirm the lib directory exists from Task 1 before creating renameEngine.ts
  </read_first>

  <behavior>
    RED phase — write all tests first, run `npm test -- --reporter=verbose renameEngine`, confirm they fail:

    - applyTemplate('{tag}-{date}-{index}', 'invoice', 3) returns string matching /^invoice-\d{4}-\d{2}-\d{2}-3$/
    - applyTemplate('{tag}-{index}', 'receipt', 0) === 'receipt-0'
    - applyTemplate('{tag}', 'screenshot', 1) === 'screenshot'
    - applyTemplate('{tag}-{date}', 'report', 5) returns string matching /^report-\d{4}-\d{2}-\d{2}$/
    - applyTemplate('fixed-name', 'any-tag', 0) === 'fixed-name'  (no slots → literal passthrough)
    - The {date} slot produces YYYY-MM-DD format (10 chars, ISO date).

    GREEN phase — implement applyTemplate.
    Run `npm test -- --reporter=verbose renameEngine` — all tests must pass before committing.
  </behavior>

  <action>
    RED commit message: "test(02-01): add failing renameEngine tests"
    GREEN commit message: "feat(02-01): implement applyTemplate"

    Implementation details (from RESEARCH.md Pattern 6):
    - date = new Date().toISOString().slice(0, 10)  (gives YYYY-MM-DD)
    - Replace '{tag}' with tag, '{date}' with date, '{index}' with String(matchCount).
    - Return the rendered string — extension is NOT appended here (per D-09, caller appends ext).
    - Function signature: export function applyTemplate(renameFormat: string, tag: string, matchCount: number): string
    - File location: src/lib/renameEngine.ts
    - No imports needed — pure function, no chrome APIs.
  </action>

  <verify>
    <automated>npm test -- --reporter=verbose renameEngine</automated>
  </verify>

  <acceptance_criteria>
    - tests/renameEngine.test.ts exists and contains at least 6 test cases covering the behaviors above.
    - src/lib/renameEngine.ts exports `applyTemplate` as a named export.
    - `npm test -- renameEngine` exits 0 with all tests passing.
    - No imports of chrome.*, @wxt-dev/storage, or any external library in renameEngine.ts.
    - The {date} slot renders as exactly 10 characters matching YYYY-MM-DD (verified by regex test).
    - The function does NOT append a file extension (test: applyTemplate('{tag}', 'x', 0) === 'x').
  </acceptance_criteria>

  <done>Both RED and GREEN commits exist. All renameEngine tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite green gate</name>
  <files></files>

  <read_first>
    - (none — this is a verification-only task; no files modified)
  </read_first>

  <action>
    Run the complete Vitest suite. All 11 existing tests (storage-schema + suggest-guard) plus all new fingerprint and renameEngine tests must pass. If any existing test regresses, fix the regression before marking this task done. Do not modify the existing test assertions — the regression is in the implementation, not the tests.
  </action>

  <verify>
    <automated>npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npm test` exits 0.
    - Suite output shows 0 failed tests.
    - tests/storage-schema.test.ts: 6 passing (unchanged from Phase 1).
    - tests/suggest-guard.test.ts: 5 passing (unchanged from Phase 1).
    - tests/fingerprint.test.ts: all passing (new).
    - tests/renameEngine.test.ts: all passing (new).
    - No TypeScript errors in the test output header.
  </acceptance_criteria>

  <done>npm test exits 0, no regressions, all new tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filename input → computeFingerprint | Untrusted string from Chrome download API — no code execution risk; pure string manipulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | STRIP_PATTERNS regex | accept | Regex operates on local string data; worst case is over-stripping (fingerprint collision) or under-stripping (cache miss). No security boundary crossed. Validated by unit tests. |
| T-02-01-SC | Tampering | npm/pip/cargo installs | accept | No new runtime packages installed in this plan. wrangler and @cloudflare/workers-types are devDeps installed in Plan 02-02. |
</threat_model>

<verification>
- `npm test` exits 0 with the full suite (existing + new tests)
- src/lib/fingerprint.ts and src/lib/renameEngine.ts exist with correct named exports
- No modifications to entrypoints/background.ts (deferred to Wave 2, Plan 02-03)
</verification>

<success_criteria>
- computeFingerprint produces stable, noise-stripped keys matching D-01/D-02/D-03 spec
- applyTemplate fills {tag}, {date}, {index} slots per D-07/D-08; extension deferred to caller per D-09
- Full Vitest suite green with no regressions
</success_criteria>

<output>
Create `.planning/phases/02-rename-engine/02-01-SUMMARY.md` when done
</output>
