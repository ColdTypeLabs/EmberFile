---
phase: "01-foundation"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - entrypoints/background.ts
  - tests/suggest-guard.test.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-05
  - QUAL-02

must_haves:
  truths:
    - "Every download triggers onDeterminingFilename and receives a [HOOK-OK-{n}]- prefixed filename"
    - "suggest() is called exactly once per download event — never zero times, never twice"
    - "When storage read throws, suggest() is still called with the original filename"
    - "Downloads complete normally — never hang"
    - "hookCounter increments sequentially within a service worker session (1, 2, 3...)"
  artifacts:
    - path: "entrypoints/background.ts"
      provides: "onDeterminingFilename listener with suggest() finally-guard"
      contains: "onDeterminingFilename.addListener"
    - path: "tests/suggest-guard.test.ts"
      provides: "Unit tests proving suggest() is always called"
  key_links:
    - from: "entrypoints/background.ts"
      to: "chrome.downloads.onDeterminingFilename"
      via: "addListener inside defineBackground()"
      pattern: "onDeterminingFilename\\.addListener"
    - from: "listener"
      to: "suggest()"
      via: "finally block with suggested boolean guard"
      pattern: "finally"
---

<objective>
Implement the `onDeterminingFilename` download hook in the background service worker. On each download: read the `enabled` flag, apply the `[HOOK-OK-{n}]-` prefix (per D-01), call `suggest()`. Always call `suggest()` in a `finally` block even when errors occur (per CORE-05, QUAL-02).

Purpose: This is the critical MV3 mechanic proof. If `suggest()` is not always called, downloads hang forever. If `onDeterminingFilename` is not wired correctly, no rename logic in any future phase will work.

Output: Background entrypoint with working hook, unit tests proving the suggest() guard fires in all paths.
</objective>

<execution_context>
@$HOME/Documents/Projects - Claude Main Folder/CLI/get-shit-done/workflows/execute-plan.md
@$HOME/Documents/Projects - Claude Main Folder/CLI/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Contracts established by Plan 01 that this plan builds on -->

From entrypoints/background.ts (produced by Plan 01):
  export const storageEnabled = storage.defineItem<boolean>('local:enabled', { fallback: true });
  // ... other defineItems ...
  export default defineBackground(() => { /* placeholder */ });

Chrome downloads API shape (from RESEARCH.md):
  chrome.downloads.onDeterminingFilename.addListener(
    (downloadItem: chrome.downloads.DownloadItem, suggest: (suggestion?: chrome.downloads.FilenameSuggestion) => void) => {
      // Must return true (or be an async function returning Promise) to signal async suggest()
    }
  );

  FilenameSuggestion shape:
    { filename: string, conflictAction?: 'uniquify' | 'overwrite' | 'prompt' }

  suggest() with no args = use Chrome's default filename
  suggest({ filename: newName }) = override filename

Architecture constraints from RESEARCH.md:
  - return true MUST be synchronous (or use async function — returns Promise which is truthy)
  - suggest() called exactly once per listener invocation
  - suggested boolean flag prevents double-call
  - hookCounter is module-level inside defineBackground() — resets on SW restart (D-02, intentional)
  - All Chrome API calls inside defineBackground() callback ONLY
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write suggest-guard tests (RED), then implement the hook (GREEN)</name>
  <files>tests/suggest-guard.test.ts, entrypoints/background.ts</files>

  <read_first>
    Read the current entrypoints/background.ts (produced by Plan 01) to understand its exact exports and defineBackground() placeholder before modifying it.
    Confirm tests/suggest-guard.test.ts does not yet exist.
  </read_first>

  <behavior>
    - Test 1: When storage read succeeds and enabled=true, suggest() is called with filename starting with '[HOOK-OK-1]-'
    - Test 2: When storage read succeeds and enabled=false, suggest() is called with original filename (no prefix)
    - Test 3: When storage read throws an error, suggest() is still called exactly once
    - Test 4: suggest() is never called more than once per download event (double-call guard)
    - Test 5: hookCounter increments — second download gets '[HOOK-OK-2]-' prefix
  </behavior>

  <action>
    STEP 1 — Create tests/suggest-guard.test.ts (write tests BEFORE modifying background.ts):

    The test file must export a handleDeterminingFilename function from background.ts for unit testing.
    Tests use fakeBrowser from @webext-core/fake-browser and vi.spyOn to mock chrome.storage.local.get.

    tests/suggest-guard.test.ts content:

      import { describe, it, expect, beforeEach, vi } from 'vitest';
      import { fakeBrowser } from '@webext-core/fake-browser';
      import { handleDeterminingFilename, resetHookCounter } from '../entrypoints/background';

      beforeEach(() => {
        fakeBrowser.reset();
        resetHookCounter();
      });

      const makeDownloadItem = (filename: string): chrome.downloads.DownloadItem =>
        ({ filename, id: 1 } as chrome.downloads.DownloadItem);

      describe('onDeterminingFilename suggest() guard', () => {
        it('prefixes filename with [HOOK-OK-1]- when enabled=true', async () => {
          await fakeBrowser.storage.local.set({ enabled: true });
          const suggest = vi.fn();

          await handleDeterminingFilename(makeDownloadItem('report.pdf'), suggest);

          expect(suggest).toHaveBeenCalledTimes(1);
          const call = suggest.mock.calls[0][0];
          expect(call?.filename).toMatch(/^\[HOOK-OK-1\]-report\.pdf$/);
        });

        it('calls suggest with original filename when enabled=false', async () => {
          await fakeBrowser.storage.local.set({ enabled: false });
          const suggest = vi.fn();

          await handleDeterminingFilename(makeDownloadItem('report.pdf'), suggest);

          expect(suggest).toHaveBeenCalledTimes(1);
          // When disabled, suggest() called with no args (Chrome uses default)
          expect(suggest.mock.calls[0][0]).toBeUndefined();
        });

        it('calls suggest exactly once when storage read throws', async () => {
          vi.spyOn(fakeBrowser.storage.local, 'get').mockRejectedValueOnce(
            new Error('storage failure')
          );
          const suggest = vi.fn();

          await handleDeterminingFilename(makeDownloadItem('report.pdf'), suggest);

          expect(suggest).toHaveBeenCalledTimes(1);
          expect(suggest.mock.calls[0][0]).toBeUndefined();
        });

        it('never calls suggest more than once (double-call guard)', async () => {
          await fakeBrowser.storage.local.set({ enabled: true });
          const suggest = vi.fn();

          await handleDeterminingFilename(makeDownloadItem('test.zip'), suggest);

          expect(suggest).toHaveBeenCalledTimes(1);
        });

        it('hookCounter increments across calls', async () => {
          await fakeBrowser.storage.local.set({ enabled: true });
          const suggest1 = vi.fn();
          const suggest2 = vi.fn();

          await handleDeterminingFilename(makeDownloadItem('a.pdf'), suggest1);
          await handleDeterminingFilename(makeDownloadItem('b.pdf'), suggest2);

          expect(suggest1.mock.calls[0][0]?.filename).toMatch(/^\[HOOK-OK-1\]-/);
          expect(suggest2.mock.calls[0][0]?.filename).toMatch(/^\[HOOK-OK-2\]-/);
        });
      });

    STEP 2 — Run tests: npx vitest run tests/suggest-guard.test.ts
    They MUST fail (RED) because handleDeterminingFilename and resetHookCounter are not exported yet.

    STEP 3 — Update entrypoints/background.ts to make tests pass (GREEN).

    The updated file must:
    a) Keep all four storageXxx defineItem exports from Plan 01 (do NOT remove them)
    b) Add a module-level hookCounter variable and resetHookCounter export (inside the module, not inside defineBackground)
    c) Export handleDeterminingFilename as a named export for unit testing
    d) Wire handleDeterminingFilename into the onDeterminingFilename listener inside defineBackground()

    Exact implementation shape:

      import { storage } from '@wxt-dev/storage';

      // --- Storage items (from Plan 01 — do not modify key names) ---
      export const storageEnabled = storage.defineItem<boolean>('local:enabled', { fallback: true });
      export const storageMonthlyCount = storage.defineItem<number>('local:monthlyCount', { fallback: 0 });
      export const storageMonthlyResetDate = storage.defineItem<string>('local:monthlyResetDate', {
        init: () => {
          const now = new Date();
          return `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-01`;
        },
      });
      export const storageRules = storage.defineItem<
        Record<string, { tag: string; renameFormat: string; matchCount: number }>
      >('local:rules', { fallback: {} });

      // --- In-memory hook counter (D-02: intentionally resets on SW restart) ---
      let hookCounter = 0;
      export const resetHookCounter = () => { hookCounter = 0; };

      // --- Extracted handler for unit testability ---
      export async function handleDeterminingFilename(
        downloadItem: chrome.downloads.DownloadItem,
        suggest: (suggestion?: chrome.downloads.FilenameSuggestion) => void
      ): Promise<void> {
        let suggested = false;

        try {
          const enabled = await storageEnabled.getValue();
          if (!enabled) return; // finally calls suggest() with no args

          hookCounter++;
          const originalName = downloadItem.filename.split(/[/\\]/).pop() ?? downloadItem.filename;
          const newName = `[HOOK-OK-${hookCounter}]-${originalName}`;

          suggest({ filename: newName, conflictAction: 'uniquify' });
          suggested = true;
        } finally {
          if (!suggested) {
            suggest(); // no-arg = Chrome uses its default filename; releases the download
          }
        }
      }

      export default defineBackground(() => {
        chrome.downloads.onDeterminingFilename.addListener(handleDeterminingFilename);
      });

    CRITICAL constraints:
    - handleDeterminingFilename must be exported (so tests can import it)
    - resetHookCounter must be exported (so tests can reset state between runs)
    - The listener registration (addListener) must be inside defineBackground() — NOT at module level
    - suggest() must be in a finally block — this is non-negotiable (CORE-05)
    - Do NOT call suggest() twice (the suggested boolean flag prevents this)
    - The split(/[/\\]/) handles both Unix and Windows path separators from Chrome's downloadItem.filename

    STEP 4 — Run both test suites to confirm GREEN:
      npx vitest run tests/suggest-guard.test.ts
      npx vitest run tests/storage-schema.test.ts
    Both must pass. If storage-schema tests broke, a Plan 01 export was accidentally removed — restore it.
  </action>

  <verify>
    <automated>npx vitest run --reporter=verbose 2>&amp;1</automated>
    Expected: All tests pass (6 from storage-schema + 5 from suggest-guard = 11 total). Exit code 0.
  </verify>

  <acceptance_criteria>
    - tests/suggest-guard.test.ts exists with 5 test cases covering: prefix format, disabled state, storage error, double-call guard, counter increment
    - entrypoints/background.ts exports handleDeterminingFilename and resetHookCounter as named exports
    - background.ts still exports all four storageXxx items from Plan 01 (no regressions)
    - chrome.downloads.onDeterminingFilename.addListener is called inside defineBackground() with handleDeterminingFilename
    - suggest() call is inside a finally block in handleDeterminingFilename
    - hookCounter is a module-level variable (not inside defineBackground or handleDeterminingFilename)
    - `npx vitest run` exits 0 with exactly 11 tests passing (6 storage-schema + 5 suggest-guard)
    - No test uses toHaveBeenCalledTimes(0) — suggest() is always called at least once
  </acceptance_criteria>

  <done>Download hook implemented with always-call suggest() guarantee. All 11 unit tests pass. Ready for manual Chrome load test.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Manual Chrome load — verify hook fires and suggest() always called</name>

  <what-built>
    The WXT extension builds to .output/chrome-mv3/. The background service worker registers an onDeterminingFilename listener that renames every download with a [HOOK-OK-{n}]- prefix and always calls suggest().
  </what-built>

  <how-to-verify>
    Run from repo root: C:\Users\maccl\Documents\Projects - Claude Main Folder\Download-Renamer-Web-Extension

    Step 1 — Build:
      npx wxt build
    Confirm: exits 0, .output/chrome-mv3/ directory exists.

    Step 2 — Load in Chrome:
      a. Open chrome://extensions
      b. Enable "Developer mode" (top-right toggle)
      c. Click "Load unpacked"
      d. Select: C:\Users\maccl\Documents\Projects - Claude Main Folder\Download-Renamer-Web-Extension\.output\chrome-mv3
      e. Confirm "Download Renamer" appears in the extension list with no errors.

    Step 3 — Test hook fires (normal path):
      a. Download any file (e.g., right-click any image → Save As, or visit a direct PDF URL)
      b. Open your Downloads folder
      c. Confirm the file has been renamed to: [HOOK-OK-1]-{original filename}
      d. Download a second file
      e. Confirm the second file is: [HOOK-OK-2]-{original filename}

    Step 4 — Test suggest() called on disable (simulated fallback path):
      a. Open chrome://extensions → click the "Details" link on Download Renamer
      b. Or: right-click extension icon → "Manage extension"
      c. Open the extension's background service worker DevTools: chrome://extensions → "Inspect views: background page" (or "Service Worker")
      d. In the DevTools console, run:
           chrome.storage.local.set({ enabled: false })
      e. Download another file
      f. Confirm the file saved with its ORIGINAL filename (no prefix) — download did not hang
      g. Restore: chrome.storage.local.set({ enabled: true })

    Step 5 — Check for CSP violations:
      a. With background DevTools still open, look at the Console tab
      b. Confirm: no "Content Security Policy" errors, no "chrome is not defined" errors

    Expected outcomes:
      - Criterion 1: Extension appears in chrome://extensions with no error badge
      - Criterion 2: Downloaded files have [HOOK-OK-1]-, [HOOK-OK-2]- prefixes in order
      - Criterion 3: Disabled mode → file saves with original name, download does not hang
      - Criterion 4: No CSP violations in DevTools console
  </how-to-verify>

  <resume-signal>
    Type "approved" if all 4 criteria pass.
    Or describe which criterion failed (e.g., "hook not firing", "CSP error: [paste error]", "download hung").

    If downloads hang: the async/Promise return may not satisfy Chrome's 'return true' requirement (RESEARCH.md open question A4).
    Fix: change handleDeterminingFilename to a non-async function with explicit `return true` and manual Promise handling.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chrome download event → background listener | Untrusted filename from Chrome's download system enters the rename path |
| background.ts → suggest() | Only valid output path; must be called exactly once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | suggest() not called (download hangs) | mitigate | finally block + suggested boolean guard; unit test T3 proves error path still calls suggest() |
| T-02-02 | Tampering | downloadItem.filename path traversal (e.g., ../../etc/passwd) | accept | Phase 1 is no-op prefix; only the basename after split(/[/\\]/) is used. Real sanitization deferred to Phase 2 when AI-suggested names are applied. |
| T-02-03 | Information Disclosure | hookCounter leaks session download count | accept | Counter is ephemeral, visible in filename. Intentional for Phase 1 debug visibility (D-01). Stripped in Phase 2. |
| T-02-SC | Tampering | npm installs | accept | All packages verified APPROVED in Plan 01 legitimacy audit. No new packages in this plan. |
</threat_model>

<verification>
After both tasks complete:
1. `npx vitest run` exits 0 with 11 tests passing
2. `npx wxt build` exits 0
3. Extension loads in Chrome without error badge
4. Three consecutive downloads produce files named [HOOK-OK-1]-*, [HOOK-OK-2]-*, [HOOK-OK-3]-*
5. Setting enabled=false → download saves with original name, does not hang
6. No CSP violations in background service worker console
</verification>

<success_criteria>
- handleDeterminingFilename exported and unit-tested (5 tests)
- suggest() always called via finally block — proven by test covering error path
- hookCounter increments per session, resets on SW restart (D-02)
- Manual Chrome load confirms hook fires visibly in filesystem
- Downloads never hang (verified via disabled-mode test)
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-02-SUMMARY.md when done
</output>
