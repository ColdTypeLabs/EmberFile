---
phase: 03-settings-ui
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - entrypoints/options/App.tsx
autonomous: true
requirements: [PATT-07, PATT-08, NOTIF-02]

must_haves:
  truths:
    - "Options page shows a 'Custom Rules' section listing all entries from local:customRules"
    - "'Add custom rule' button opens a modal with match text input, rename format input, and slot hint"
    - "Submitting the modal writes to storageCustomRules and closes the modal; new rule appears in list"
    - "Options page checks local:pendingConflict on mount; if non-null shows ConflictModal before anything else"
    - "ConflictModal shows both custom and learned renameFormat side by side with 'Use this' buttons"
    - "Picking 'Use this' writes the chosen renameFormat to storageRules[fingerprint], clears storageConflict, and closes the modal"
    - "ConflictModal has no dismiss/cancel — user must pick one option"
  artifacts:
    - path: "entrypoints/options/App.tsx"
      provides: "Custom Rules section, CustomRuleModal, ConflictModal"
  key_links:
    - from: "CustomRuleModal submit"
      to: "storageCustomRules"
      via: "storageCustomRules.getValue() then setValue with new entry keyed by matchText"
      pattern: "storageCustomRules.setValue"
    - from: "ConflictModal pick"
      to: "storageRules"
      via: "storageRules.getValue() then setValue updating fingerprint renameFormat to chosen format"
      pattern: "storageRules.setValue"
    - from: "ConflictModal pick"
      to: "storageConflict"
      via: "storageConflict.setValue(null) to clear after resolution"
      pattern: "storageConflict.setValue"
---

<objective>
Add the Custom Rules section (with AddCustomRuleButton and CustomRuleModal) and the ConflictModal to options/App.tsx. This completes the options page and delivers full PATT-07, PATT-08, and NOTIF-02.

Purpose: After this plan the user can create custom rename rules without downloading a file first, and resolve any queued conflict between a custom and learned rule — all five Phase 3 success criteria are met.

Output: Custom Rules section with working Add modal. ConflictModal appears on mount when local:pendingConflict is non-null.
</objective>

<execution_context>
@C:\Users\maccl\Documents\Projects - Claude Main Folder\CLI\get-shit-done\workflows\execute-plan.md
@C:\Users\maccl\Documents\Projects - Claude Main Folder\CLI\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-settings-ui/03-CONTEXT.md
@.planning/phases/03-settings-ui/03-UI-SPEC.md
</context>

<interfaces>
From src/lib/storage.ts:
  storageCustomRules: WxtStorageItem&lt;Record&lt;string, { matchText: string; renameFormat: string }&gt;&gt;
  storageConflict: WxtStorageItem&lt;{ fingerprint: string; customRule: { matchText: string; renameFormat: string }; learnedRule: { tag: string; renameFormat: string } } | null&gt;
  storageRules: WxtStorageItem&lt;Record&lt;string, { tag: string; renameFormat: string; matchCount: number }&gt;&gt;

From lucide-react:
  Plus -- icon for AddCustomRuleButton
  X -- icon for modal close (not used in ConflictModal — no dismiss; used in CustomRuleModal cancel button only as an icon alternative)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Custom Rules section and CustomRuleModal to options/App.tsx</name>
  <read_first>
    - entrypoints/options/App.tsx (current state after 03-04 — must read to understand insertion point and avoid breaking existing sections)
    - .planning/phases/03-settings-ui/03-UI-SPEC.md (CustomRuleRow, AddCustomRuleButton, CustomRuleModal specs, custom rule modal copywriting contract, empty state for custom rules, interaction states for CustomRuleModal)
    - .planning/phases/03-settings-ui/03-CONTEXT.md (D-17, D-18, D-19 — separate section, filename-contains match, modal for add)
  </read_first>
  <action>
    Add the "Custom Rules" section below the Learned Rules section in options/App.tsx. Preserve all existing sections.

    SectionHeading for custom rules: h2 "Custom Rules" with className "text-base font-semibold text-gray-900 mb-4". Per D-17.

    CustomRuleRow: similar structure to RuleRow but displays 'contains "{matchText}" → {renameFormat}' on the left (className "text-sm text-gray-900"). Right side: Trash2 icon button (aria-label "Delete custom rule"). On delete: show same inline red confirm pattern as learned rules — "Delete this rule?" / "Yes, delete" / "Cancel". On confirm: storageCustomRules.getValue(), delete the key (matchText is the key), storageCustomRules.setValue().

    EmptyRuleState for custom rules: h3 "No custom rules" + p "Add a custom rule to override or supplement learned rules." Per UI-SPEC empty states.

    AddCustomRuleButton: a button below the rule list with className "bg-blue-600 text-white text-sm font-semibold px-4 py-2 rounded-md h-9 hover:bg-blue-700 transition-colors flex items-center gap-2". Prepend Plus icon (size 16). Text: "Add custom rule". On click: set showCustomRuleModal = true. Per D-19.

    CustomRuleModal: rendered when showCustomRuleModal is true. Modal overlay: div with className "fixed inset-0 bg-black/50 flex items-center justify-center z-50". Modal card: div with className "bg-white rounded-lg p-6 max-w-sm w-full mx-4 flex flex-col gap-4". Per UI-SPEC modal dimensions.

    Modal contents:
    - h2 "Add custom rule" with className "text-base font-semibold text-gray-900".
    - Match text field: label "If filename contains" (className "text-sm font-semibold text-gray-700") above an input with placeholder "e.g. invoice", className "border border-gray-200 rounded px-2 py-1 text-sm w-full focus:outline-none focus:ring-2 focus:ring-blue-500".
    - Rename format field: label "Rename to" above input with placeholder "e.g. Invoice_{date}", same input className.
    - Slot hint: p "Slots: {tag} {date} {index}" with className "text-xs text-gray-400".
    - Button row (flex gap-2 justify-end): "Discard" button (className "text-sm font-semibold text-gray-500 px-3 py-1 hover:text-gray-700") and "Add rule" button (className "bg-blue-600 text-white text-sm font-semibold px-4 py-2 rounded-md h-9 hover:bg-blue-700 disabled:opacity-50").
    - Discard: close modal, reset fields.
    - Add rule: validate both fields are non-empty. If either is empty, show a validation message "Both fields are required." in text-xs text-red-600. If valid: storageCustomRules.getValue(), set entry keyed by matchText to { matchText, renameFormat }, storageCustomRules.setValue(). On success: update local customRules state, close modal, clear fields. On error: "Could not save rule. Please try again." in text-xs text-red-600.

    The customRules state is loaded in the parent App component mount useEffect alongside the existing storage reads. Add storageCustomRules.getValue() to the Promise.all call added in 03-03.

    Import Plus from lucide-react (add to the existing lucide-react import line).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - entrypoints/options/App.tsx contains "Custom Rules" string as section heading (D-17)
    - entrypoints/options/App.tsx contains "Add custom rule" string on button (D-19)
    - entrypoints/options/App.tsx contains "If filename contains" label (D-18, UI-SPEC modal copy)
    - entrypoints/options/App.tsx contains "Rename to" label (UI-SPEC modal copy)
    - entrypoints/options/App.tsx contains "Add rule" and "Discard" strings in modal button row (UI-SPEC)
    - entrypoints/options/App.tsx contains "No custom rules" string (empty state)
    - entrypoints/options/App.tsx contains "storageCustomRules.setValue" (write on add)
    - entrypoints/options/App.tsx imports "Plus" from "lucide-react"
    - npm run build exits 0
  </acceptance_criteria>
  <done>Custom rules section renders. User can add custom rules via modal (writes to local:customRules).</done>
</task>

<task type="auto">
  <name>Task 2: Add ConflictModal — checks local:pendingConflict on mount and requires resolution</name>
  <read_first>
    - entrypoints/options/App.tsx (current state after Task 1 — must read to find mount useEffect and insert conflict check)
    - .planning/phases/03-settings-ui/03-UI-SPEC.md (ConflictModal component spec, conflict modal copywriting contract, interaction state: "not shown → shown on mount → user picks → cleared")
    - .planning/phases/03-settings-ui/03-CONTEXT.md (D-20, D-21, D-22, D-23 — conflict schema, fallback, one-at-a-time, resolution UX)
  </read_first>
  <action>
    Add storageConflict.getValue() to the mount useEffect's Promise.all call. Store the result in a React state variable (e.g. pendingConflict). If non-null, render ConflictModal.

    ConflictModal: rendered when pendingConflict is non-null. Modal overlay: same className as CustomRuleModal ("fixed inset-0 bg-black/50 flex items-center justify-center z-50"). Modal card: "bg-white rounded-lg p-6 max-w-sm w-full mx-4 flex flex-col gap-4".

    Modal contents per UI-SPEC copywriting contract:
    - h2 "Rule conflict detected" with className "text-base font-semibold text-gray-900".
    - p "A custom rule and a learned rule both match this download. Pick which format to use going forward." className "text-sm text-gray-500".
    - Two side-by-side cards (flex gap-4): left card shows "Custom rule" label (text-xs font-semibold text-gray-500 uppercase tracking-wide) above the customRule.renameFormat in text-sm text-gray-900, with a "Use this" button (className "bg-blue-600 text-white text-sm font-semibold px-3 py-1 rounded h-9 hover:bg-blue-700 w-full mt-2 disabled:opacity-50"). Right card mirrors this for "Learned rule" / learnedRule.renameFormat.
    - No dismiss or cancel button. The modal is only closeable by clicking "Use this" on one of the options. Per D-23.

    "Use this" (custom rule) behavior:
    1. Disable both buttons while saving.
    2. storageRules.getValue() — update the rule at pendingConflict.fingerprint, setting renameFormat to pendingConflict.customRule.renameFormat. storageRules.setValue().
    3. storageConflict.setValue(null).
    4. On success: set pendingConflict state to null (modal disappears). Update local rules state with the new renameFormat so the learned rules list reflects the change.
    5. On error: show "Could not save choice. Please try again." below buttons in text-xs text-red-600.

    "Use this" (learned rule) behavior:
    1. Disable both buttons while saving.
    2. The learned rule's renameFormat is already in storageRules — no rules update needed.
    3. storageConflict.setValue(null).
    4. On success: set pendingConflict state to null.
    5. On error: same error message.

    The ConflictModal renders above CustomRuleModal in z-order (z-50 for both is fine since ConflictModal is higher in the component tree and rendered earlier in the JSX).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - entrypoints/options/App.tsx contains "Rule conflict detected" string (UI-SPEC conflict modal title)
    - entrypoints/options/App.tsx contains "A custom rule and a learned rule both match" string (UI-SPEC modal body)
    - entrypoints/options/App.tsx contains "Custom rule" and "Learned rule" option labels (UI-SPEC)
    - entrypoints/options/App.tsx contains "Use this" string (conflict pick button per UI-SPEC)
    - entrypoints/options/App.tsx contains "storageConflict" imports and usage
    - entrypoints/options/App.tsx contains "storageConflict.setValue(null)" (conflict clear on resolution)
    - entrypoints/options/App.tsx contains "storageConflict.getValue()" in the mount useEffect
    - ConflictModal has NO cancel or dismiss button text in the component (no "Cancel", no "Close", no "X" button — modal forces resolution per D-23)
    - npm run build exits 0
  </acceptance_criteria>
  <done>ConflictModal appears on options page mount when local:pendingConflict is non-null. User must pick one rule; choice is written to storageRules and conflict is cleared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| options UI → storageCustomRules | User provides matchText and renameFormat — stored in local storage, matchText used as .includes() substring only |
| options UI → storageRules | Conflict resolution writes chosen renameFormat to rules — same template-only risk as edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | CustomRuleModal matchText input | accept | matchText stored in local:customRules and used only as .includes() in background.ts — no eval or regex construction |
| T-03-10 | Denial of Service | ConflictModal no-dismiss | accept | By design (D-23) — forcing resolution prevents conflict accumulation; one conflict at a time (D-22) |
| T-03-11 | Information Disclosure | ConflictModal showing renameFormat | accept | Shown only to the extension user in their own browser; no external exposure |
</threat_model>

<verification>
- npm run build exits 0
- All five Phase 3 success criteria are testable in the built extension:
  1. Popup shows rename count + Pause/Resume + FREE badge (03-02)
  2. Options page lists learned rules with edit/delete (03-04)
  3. Inline edit saves forward only (03-04)
  4. Custom rule modal adds to local:customRules (this plan Task 1)
  5. ConflictModal resolves and clears local:pendingConflict (this plan Task 2)
</verification>

<success_criteria>
- User can create custom rules without downloading a file (PATT-07)
- When both a custom and learned rule match a download, the conflict is queued and the conflict modal appears on next options page visit (PATT-08, NOTIF-02)
- User picks one rule in the modal; choice is stored; modal never shows again for that pattern
</success_criteria>

<output>
Create .planning/phases/03-settings-ui/03-05-SUMMARY.md when done.
</output>
