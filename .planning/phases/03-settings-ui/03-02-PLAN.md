---
phase: 03-settings-ui
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - entrypoints/popup/App.tsx
autonomous: true
requirements: [SET-01, SET-02, SET-03]

must_haves:
  truths:
    - "Popup renders a 360px white card with Download Renamer header"
    - "Pause/Resume button is full-width blue-600, changes label based on local:enabled value"
    - "Rename count reads local:monthlyCount and renders '{N} files renamed this month'"
    - "FREE or PREMIUM badge renders based on a placeholder isPremium flag (false for now)"
    - "Manage rules → link opens options.html in a new tab via chrome.tabs.create"
  artifacts:
    - path: "entrypoints/popup/App.tsx"
      provides: "Full popup UI with all components per UI-SPEC"
      exports: ["default App"]
  key_links:
    - from: "PauseResumeButton"
      to: "storageEnabled"
      via: "storageEnabled.getValue() on mount, storageEnabled.setValue() on click"
      pattern: "storageEnabled"
    - from: "RenameCountLabel"
      to: "storageMonthlyCount"
      via: "storageMonthlyCount.getValue() on mount"
      pattern: "storageMonthlyCount"
---

<objective>
Replace the stub entrypoints/popup/App.tsx with the full popup UI — all components from the UI-SPEC delivered as a vertical slice: storage reads, React state, and rendered UI wired end-to-end.

Purpose: After this plan the user can open the extension popup and see their rename count, pause/resume the extension, see their account badge, and navigate to the options page.

Output: A working popup with PopupShell, PopupHeader, PauseResumeButton, RenameCountLabel, AccountBadge, and ManageRulesLink.
</objective>

<execution_context>
@C:\Users\maccl\Documents\Projects - Claude Main Folder\CLI\get-shit-done\workflows\execute-plan.md
@C:\Users\maccl\Documents\Projects - Claude Main Folder\CLI\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-settings-ui/03-CONTEXT.md
@.planning/phases/03-settings-ui/03-UI-SPEC.md
</context>

<interfaces>
From src/lib/storage.ts (created in 03-01):
  storageEnabled: WxtStorageItem&lt;boolean&gt; — getValue() / setValue(v: boolean)
  storageMonthlyCount: WxtStorageItem&lt;number&gt; — getValue()
  (import from '../../src/lib/storage')

From src/lib/renameEngine.ts:
  applyTemplate(renameFormat: string, tag: string, matchCount: number): string

Chrome API available in popup context:
  chrome.tabs.create({ url: string }): void
  chrome.runtime.getURL(path: string): string
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build full popup UI in entrypoints/popup/App.tsx</name>
  <read_first>
    - entrypoints/popup/App.tsx (current stub to replace)
    - entrypoints/popup/main.tsx (verify tailwind.css import added in 03-01)
    - .planning/phases/03-settings-ui/03-UI-SPEC.md (component inventory, copywriting contract, color tokens, interaction states — executor must read every section)
    - .planning/phases/03-settings-ui/03-CONTEXT.md (D-01 through D-07 locked decisions)
    - src/lib/storage.ts (storage item names and types)
  </read_first>
  <action>
    Rewrite entrypoints/popup/App.tsx as a single-file React component implementing all six popup components inline (no separate files needed for this small scope):

    PopupShell: outer div with w-[360px] bg-white p-4 flex flex-col gap-3. This is the fixed-width constraint from D-01.

    PopupHeader: a row (flex items-center gap-2) containing an img or svg placeholder icon (20x20) and a span "Download Renamer" with className "text-lg font-bold text-gray-900". Per D-05.

    PauseResumeButton: a button with className "w-full bg-blue-600 text-white rounded-md h-9 text-sm font-semibold hover:bg-blue-700 transition-colors". Label: "Pause" when enabled is true, "Resume" when enabled is false. Per D-02. On click: toggle enabled, call storageEnabled.setValue(!enabled). Per UI-SPEC, this button renders ABOVE the stats line — it is the primary visual anchor.

    RenameCountLabel: a p tag with className "text-sm text-gray-500". Content: "{count} files renamed this month". count comes from storageMonthlyCount.getValue(). Per D-06.

    AccountBadge: a span pill. FREE state: className "bg-blue-100 text-blue-700 text-xs font-semibold px-2 py-0.5 rounded-full". PREMIUM state: className "bg-indigo-100 text-indigo-700 text-xs font-semibold px-2 py-0.5 rounded-full". Text: "FREE" or "PREMIUM ✓". In Phase 3 the isPremium flag is always false (hardcoded) — Phase 4 wires the real license check. Per SET-03.

    ManageRulesLink: a button with className "text-blue-600 text-sm font-semibold text-left". Text: "Manage rules →". On click: chrome.tabs.create({ url: chrome.runtime.getURL('options.html') }). Per D-03 and D-08.

    State management: use a single useEffect on mount to call storageEnabled.getValue() and storageMonthlyCount.getValue() concurrently via Promise.all. Store results in React state (useState). Show a loading skeleton or null while data loads to avoid flash of incorrect state. The enabled/disabled toggle writes to storage immediately on click then updates local state.

    Import lucide-react icons are NOT needed in the popup (the UI-SPEC only uses Pencil, Trash2, X, Check, Plus in the options page). Do not import lucide-react in App.tsx unless using an icon.

    Do not use font-bold anywhere except the "Download Renamer" display heading — per UI-SPEC typography weight clarification.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - entrypoints/popup/App.tsx does NOT import from entrypoints/background.ts (must import from ../../src/lib/storage)
    - entrypoints/popup/App.tsx contains "storageEnabled" and "storageMonthlyCount"
    - entrypoints/popup/App.tsx contains "w-[360px]" (D-01 popup width)
    - entrypoints/popup/App.tsx contains "bg-blue-600" on the Pause/Resume button
    - entrypoints/popup/App.tsx contains "Pause" and "Resume" string literals (D-02)
    - entrypoints/popup/App.tsx contains "files renamed this month" string (D-06)
    - entrypoints/popup/App.tsx contains "Manage rules" string and chrome.tabs.create call (D-03)
    - entrypoints/popup/App.tsx contains "options.html" in the chrome.runtime.getURL call
    - entrypoints/popup/App.tsx contains "FREE" and "PREMIUM ✓" strings (SET-03)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Popup renders rename count, Pause/Resume button, FREE badge, and Manage rules link. All storage reads are from src/lib/storage.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| popup → storage | Reads enabled flag and count from local storage — trusted extension origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | storageEnabled | accept | Popup bypass is accepted in v1 per STATE.md decision — Phase 4 adds backend enforcement |
| T-03-04 | Spoofing | isPremium hardcoded false | accept | Client-side premium flag is v1 limitation; noted in REQUIREMENTS.md MON-03 |
</threat_model>

<verification>
- npm run build exits 0
- popup.html builds with Tailwind utility class output visible in dist CSS
</verification>

<success_criteria>
- Popup shows correct rename count from local:monthlyCount
- Pause/Resume button reads and writes local:enabled
- FREE badge displays (isPremium=false hardcoded)
- "Manage rules →" opens options.html in a new tab
</success_criteria>

<output>
Create .planning/phases/03-settings-ui/03-02-SUMMARY.md when done.
</output>
