---
phase: 03-settings-ui
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - entrypoints/options/App.tsx
autonomous: true
requirements: [PATT-04, PATT-05, PATT-06, SET-05]

must_haves:
  truths:
    - "Options page shows a 'Learned Rules' section listing all rules from local:rules"
    - "Each rule row displays '{tag} → {renameFormat}' with a pencil icon and trash icon"
    - "Clicking pencil expands an edit row below the rule with a text input pre-filled with renameFormat"
    - "Save format writes the updated renameFormat to storage; row collapses back to default state"
    - "Clicking trash shows an inline red confirmation row; 'Yes, delete' removes the rule from storage"
    - "Empty state renders when there are no learned rules"
  artifacts:
    - path: "entrypoints/options/App.tsx"
      provides: "Learned Rules section with RuleRow, EditExpandedRow, DeleteConfirmRow, EmptyRuleState"
  key_links:
    - from: "EditExpandedRow save"
      to: "storageRules"
      via: "storageRules.getValue() then setValue with updated renameFormat"
      pattern: "storageRules.setValue"
    - from: "DeleteConfirmRow yes"
      to: "storageRules"
      via: "storageRules.getValue() then delete key then setValue"
      pattern: "storageRules.setValue"
---

<objective>
Add the Learned Rules section to options/App.tsx — RuleRow, EditExpandedRow, DeleteConfirmRow, EmptyRuleState — all wired to live storage reads and writes. Full vertical slice: list renders, edits save, deletes confirm then remove.

Purpose: After this plan the user can view all their learned rules, fix a rename format, and delete unwanted rules — the core rule management workflow.

Output: Learned Rules section in options/App.tsx with all interaction states working end-to-end.
</objective>

<execution_context>
@C:\Users\maccl\Documents\Projects - Claude Main Folder\CLI\get-shit-done\workflows\execute-plan.md
@C:\Users\maccl\Documents\Projects - Claude Main Folder\CLI\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-settings-ui/03-CONTEXT.md
@.planning/phases/03-settings-ui/03-UI-SPEC.md
</context>

<interfaces>
From src/lib/storage.ts:
  storageRules: WxtStorageItem&lt;Record&lt;string, { tag: string; renameFormat: string; matchCount: number }&gt;&gt;
  storageCustomRules: WxtStorageItem&lt;Record&lt;string, { matchText: string; renameFormat: string }&gt;&gt;

From src/lib/renameEngine.ts:
  applyTemplate(renameFormat: string, tag: string, matchCount: number): string
  -- use for live preview below the edit input (shows what the format produces for this tag)

From lucide-react:
  Pencil, Trash2 -- icons for rule row actions (size 16, className "text-gray-400 hover:text-gray-700")
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Learned Rules section with RuleRow, EditExpandedRow, DeleteConfirmRow, EmptyRuleState</name>
  <read_first>
    - entrypoints/options/App.tsx (current state after 03-03 — must read to understand where to insert and preserve existing sections)
    - .planning/phases/03-settings-ui/03-UI-SPEC.md (full component specs, interaction states table, copywriting contract for rule row, delete confirm, empty state, error states — executor must read all of these)
    - .planning/phases/03-settings-ui/03-CONTEXT.md (D-12, D-13, D-14, D-15, D-16 — inline edit, slot hint, delete confirm behavior)
    - src/lib/renameEngine.ts (applyTemplate signature)
  </read_first>
  <action>
    Add the "Learned Rules" section to options/App.tsx below the AccountSection placeholder comment. The existing shell (header, stats, account) must remain intact.

    SectionHeading: reusable h2 with className "text-base font-semibold text-gray-900 mb-4". Text: "Learned Rules".

    RuleList: a ul with className "flex flex-col divide-y divide-gray-100". Renders one RuleRow per fingerprint in the rules object. If rules object has 0 keys, renders EmptyRuleState instead.

    RuleRow (default state): a li with className "flex items-center justify-between py-3 px-2 hover:bg-gray-50 rounded". Left side: span with className "text-sm text-gray-900" showing "{tag} → {renameFormat}" (use the tag and renameFormat fields from the rule value). Right side: two icon buttons — Pencil (aria-label "Edit rename format") and Trash2 (aria-label "Delete rule"), each with className "p-1 rounded hover:bg-gray-100 text-gray-400 hover:text-gray-700 transition-colors". Clicking Pencil sets this row's mode to "editing". Clicking Trash2 sets this row's mode to "deleting".

    Row mode state: manage per-row state with a React state object of shape { [fingerprint]: 'default' | 'editing' | 'deleting' }. Default is 'default' for all rows.

    EditExpandedRow (mode = "editing"): renders in place of the default row content (not below it — replaces the right-side icons with the expanded form). Contains:
    - A text input pre-filled with the current renameFormat, className "border border-gray-200 rounded px-2 py-1 text-sm w-full focus:outline-none focus:ring-2 focus:ring-blue-500".
    - A slot hint line: p with className "text-xs text-gray-400 mt-1" and text "Slots: {tag} {date} {index}". Per D-15.
    - A live preview line: p with className "text-xs text-gray-500 mt-1" showing "Preview: " + applyTemplate(currentInputValue, rule.tag, rule.matchCount). Import applyTemplate from src/lib/renameEngine.ts.
    - Two buttons in a flex row: "Save format" (className "bg-blue-600 text-white text-sm font-semibold px-3 py-1 rounded h-9 hover:bg-blue-700 disabled:opacity-50") and "Discard" (className "text-sm font-semibold text-gray-500 px-3 py-1 hover:text-gray-700"). Per UI-SPEC copywriting.
    - Save format behavior: disable button while saving. Call storageRules.getValue(), update the renameFormat for the fingerprint, call storageRules.setValue(). On success: update local rules state, set row mode back to 'default'. On error: show inline "Could not save changes. Please try again." in text-red-600 text-xs. Per D-13 (only renameFormat is editable, not tag).
    - Discard behavior: reset input to original renameFormat, set row mode to 'default'.

    DeleteConfirmRow (mode = "deleting"): replaces the row content with className "bg-red-50 rounded px-2 py-3 flex items-center justify-between". Left: span with className "text-sm text-red-700" and text "Delete this rule?". Right: two buttons — "Yes, delete" (className "bg-red-600 text-white text-sm font-semibold px-3 py-1 rounded h-9 hover:bg-red-700 disabled:opacity-50") and "Cancel" (className "text-sm font-semibold text-gray-500 px-3 py-1"). Per D-16.
    - Yes, delete behavior: disable buttons while deleting. Call storageRules.getValue(), delete the fingerprint key, call storageRules.setValue(). On success: update local rules state removing that fingerprint. On error: show "Could not delete rule. Please try again." in text-red-600 text-xs.
    - Cancel: set row mode to 'default'.

    EmptyRuleState: a div with className "text-center py-8". h3 "No learned rules yet" in text-sm font-semibold text-gray-900. p "Rules are created automatically the first time you download a new file type." in text-sm text-gray-500 mt-1. Per UI-SPEC empty states contract.

    The storageRules data is already loaded in the parent App component state from the mount useEffect (established in 03-03). Reference that state — do not re-fetch in the section component. Update the local state copy on successful saves/deletes so the UI reflects changes immediately without re-reading storage.

    Import Pencil and Trash2 from lucide-react at the top of options/App.tsx.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - entrypoints/options/App.tsx contains "Learned Rules" string as section heading (SET-05)
    - entrypoints/options/App.tsx contains "Edit rename format" (pencil aria-label per UI-SPEC)
    - entrypoints/options/App.tsx contains "Delete rule" (trash aria-label per UI-SPEC)
    - entrypoints/options/App.tsx contains "Save format" and "Discard" strings (edit row copy per UI-SPEC)
    - entrypoints/options/App.tsx contains "Delete this rule?" string (delete confirm copy per UI-SPEC)
    - entrypoints/options/App.tsx contains "Yes, delete" string (D-16)
    - entrypoints/options/App.tsx contains "No learned rules yet" string (empty state)
    - entrypoints/options/App.tsx contains "Slots: {tag} {date} {index}" string (D-15 slot hint)
    - entrypoints/options/App.tsx imports "Pencil" and "Trash2" from "lucide-react"
    - entrypoints/options/App.tsx contains "applyTemplate" import from renameEngine (live preview)
    - entrypoints/options/App.tsx contains "storageRules.setValue" (edit save and delete write)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Learned rules section renders. User can edit rename format inline (saves to storage) and delete rules with inline confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| options UI → storageRules | User edits renameFormat — stored in local storage, used as a template string (not eval'd) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | renameFormat edit input | accept | renameFormat is passed to applyTemplate which uses only .replace() — no eval, no template literal injection risk |
| T-03-08 | Denial of Service | delete rule confirmation | accept | Inline double-confirmation (Delete this rule? / Yes, delete) prevents accidental data loss |
</threat_model>

<verification>
- npm run build exits 0
- "Learned Rules" section visible in options page
- RuleRow renders "{tag} → {renameFormat}" for each key in local:rules
</verification>

<success_criteria>
- User can view all learned rules (PATT-04, SET-05)
- User can delete a rule with inline confirm (PATT-05)
- User can edit renameFormat inline; save writes to storage; discard reverts (PATT-06)
</success_criteria>

<output>
Create .planning/phases/03-settings-ui/03-04-SUMMARY.md when done.
</output>
