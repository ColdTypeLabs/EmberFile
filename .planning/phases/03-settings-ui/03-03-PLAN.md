---
phase: 03-settings-ui
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - entrypoints/options/App.tsx
  - entrypoints/options/main.tsx
autonomous: true
requirements: [SET-03, SET-04]

must_haves:
  truths:
    - "Options page renders full-tab layout with Download Renamer header"
    - "Stats section shows learned rule count and monthly rename count read from storage"
    - "Account section shows FREE/PREMIUM badge and Upgrade to Premium button for free users"
    - "Upgrade button opens an external URL in a new tab"
    - "Page layout is single scrolling column, max-w-2xl centered, bg-white"
  artifacts:
    - path: "entrypoints/options/App.tsx"
      provides: "Options page shell with stats and account sections"
      exports: ["default App"]
  key_links:
    - from: "StatsSection"
      to: "storageRules"
      via: "Object.keys(rules).length for rule count"
      pattern: "storageRules"
    - from: "StatsSection"
      to: "storageMonthlyCount"
      via: "storageMonthlyCount.getValue()"
      pattern: "storageMonthlyCount"
---

<objective>
Replace the placeholder options/App.tsx with a real options page shell: OptionsShell, OptionsHeader, StatsSection, and AccountSection. This is a vertical slice — real storage reads, real layout, real copy.

Purpose: After this plan the user can open the options page and see their stats and account tier. This is the foundation the rule list plans (03-04, 03-05) build on top of.

Output: Options page with header, stats (rule count + monthly renames), and account badge with upgrade button.
</objective>

<execution_context>
@C:\Users\maccl\Documents\Projects - Claude Main Folder\CLI\get-shit-done\workflows\execute-plan.md
@C:\Users\maccl\Documents\Projects - Claude Main Folder\CLI\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-settings-ui/03-CONTEXT.md
@.planning/phases/03-settings-ui/03-UI-SPEC.md
</context>

<interfaces>
From src/lib/storage.ts (created in 03-01):
  storageRules: WxtStorageItem&lt;Record&lt;string, { tag: string; renameFormat: string; matchCount: number }&gt;&gt;
  storageMonthlyCount: WxtStorageItem&lt;number&gt;
  (import from '../../src/lib/storage')

Chrome API:
  chrome.tabs.create({ url: string }): void  -- for Upgrade button
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build OptionsShell, OptionsHeader, StatsSection, AccountSection in options/App.tsx</name>
  <read_first>
    - entrypoints/options/App.tsx (placeholder from 03-01 to replace)
    - entrypoints/options/main.tsx (verify tailwind.css import is present — add if missing)
    - .planning/phases/03-settings-ui/03-UI-SPEC.md (component inventory for options page, copywriting contract for stats/account sections, color and typography tokens)
    - .planning/phases/03-settings-ui/03-CONTEXT.md (D-08 through D-12)
    - src/lib/storage.ts (storageRules, storageMonthlyCount)
  </read_first>
  <action>
    Rewrite entrypoints/options/App.tsx (replacing the placeholder) with the options page shell.

    OptionsShell: outer div with className "min-h-screen bg-white". Inner container: div with className "max-w-2xl mx-auto px-6 py-8 flex flex-col gap-8". Per D-09.

    OptionsHeader: a row (flex items-center gap-2 mb-2) containing a 20x20 icon placeholder and "Download Renamer" in className "text-lg font-bold text-gray-900". Identical branding to popup per D-10. font-bold used ONLY on this heading.

    StatsSection: renders two lines of text-sm text-gray-500:
    - "{ruleCount} learned rules" — ruleCount = Object.keys(rules).length from storageRules
    - "{monthlyCount} files renamed this month" — from storageMonthlyCount
    Per SET-04 copywriting contract.

    AccountSection: heading "Account" in SectionHeading style (text-base font-semibold text-gray-900 mb-4). Below: the FREE/PREMIUM badge (same pill component as popup: bg-blue-100 text-blue-700 or bg-indigo-100 text-indigo-700). For free users only: an "Upgrade to Premium" button with className "bg-indigo-600 text-white text-sm font-semibold px-4 py-2 rounded-md h-9 hover:bg-indigo-700 transition-colors". On click: chrome.tabs.create({ url: 'https://example.com/upgrade' }) — placeholder URL, Phase 4 replaces. Per D-11, SET-03.

    Leave a placeholder comment below AccountSection: {/* Rule sections go here — 03-04, 03-05 */}

    State: single useEffect on mount, Promise.all reading storageRules.getValue() and storageMonthlyCount.getValue(). isPremium is hardcoded false (same as popup). Show a loading state (text-sm text-gray-400 "Loading...") while storage reads are pending.

    Add src/assets/tailwind.css import to entrypoints/options/main.tsx if not already present (check before adding to avoid duplicate).

    Do not apply font-bold to AccountSection heading or any element other than "Download Renamer" per UI-SPEC weight clarification.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - entrypoints/options/App.tsx contains "max-w-2xl mx-auto" (OptionsShell centering per D-09)
    - entrypoints/options/App.tsx contains "text-lg font-bold" only once (on the "Download Renamer" heading)
    - entrypoints/options/App.tsx contains "learned rules" string (SET-04 stats copy)
    - entrypoints/options/App.tsx contains "files renamed this month" string (SET-04)
    - entrypoints/options/App.tsx contains "Account" string as a section heading (SET-03)
    - entrypoints/options/App.tsx contains "Upgrade to Premium" string (SET-03, D-11)
    - entrypoints/options/App.tsx contains "FREE" and "PREMIUM ✓" strings
    - entrypoints/options/App.tsx imports from "../../src/lib/storage" (NOT from entrypoints/background.ts)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Options page shows header, live stats from storage, and account badge with upgrade button.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| options page → storage | Reads rules and count from local storage — extension origin only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | storageRules count display | accept | Rule count is user's own data shown to themselves; no PII exposure |
| T-03-06 | Tampering | Upgrade URL | accept | Placeholder URL in v1; Phase 4 replaces with real payment URL |
</threat_model>

<verification>
- npm run build exits 0 with options.html in output
- Options page renders in browser with correct Tailwind styles
</verification>

<success_criteria>
- Options page shows "{N} learned rules" and "{N} files renamed this month" from live storage
- Account section shows FREE badge and "Upgrade to Premium" button
- Layout is max-w-2xl centered single column
</success_criteria>

<output>
Create .planning/phases/03-settings-ui/03-03-SUMMARY.md when done.
</output>
