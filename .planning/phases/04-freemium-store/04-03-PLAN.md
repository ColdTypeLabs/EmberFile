---
phase: 04-freemium-store
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - entrypoints/popup/App.tsx
  - tests/popup-freemium.test.tsx
autonomous: true
requirements: [MON-02]
user_setup: []

must_haves:
  truths:
    - "When storageLocalLicenseKey is null and monthlyCount >= 5, an amber upgrade banner renders in PopupScreen below the brand header"
    - "When storageLocalLicenseKey is a non-empty string (premium), the upgrade banner is not rendered even if count >= 5"
    - "PopupScreen footer shows 'PREMIUM ✓' when isPremium is true; shows the FREE badge + 'Upgrade →' link when false"
    - "Upgrade banner 'Upgrade to Premium' button calls chrome.tabs.create({ url: UPGRADE_URL }) when clicked"
    - "isPremium state is derived from storageLocalLicenseKey at popup mount time via the existing Promise.all useEffect"
  artifacts:
    - path: "entrypoints/popup/App.tsx"
      provides: "isPremium wiring + at-limit upgrade banner in PopupScreen, already implemented"
      contains: "showUpgradeBanner"
    - path: "tests/popup-freemium.test.tsx"
      provides: "Regression coverage for the freemium UI states described above"
      contains: "showUpgradeBanner"
  key_links:
    - from: "entrypoints/popup/App.tsx"
      to: "src/lib/storage.ts"
      via: "import storageLocalLicenseKey"
      pattern: "storageLocalLicenseKey"
    - from: "entrypoints/popup/App.tsx"
      to: "src/lib/constants.ts"
      via: "import UPGRADE_URL"
      pattern: "UPGRADE_URL"
---

<objective>
Reconciliation plan — NOT new implementation. A prior ad-hoc popup UI redesign (tracked in STATE.md, executed outside the normal 04-03 plan-execute loop) already consolidated all Phase 4 popup work directly into `entrypoints/popup/App.tsx`: `isPremium` wiring, the at-limit upgrade banner, and the FREE/PREMIUM footer badge. The original 04-03-PLAN.md (now superseded) assumed a `RenameCountLabel` component and a separate `UpgradeBanner` component — neither exists; the real implementation is inline JSX inside `PopupScreen`.

This plan verifies the already-built popup freemium UI against the goal-backward must-haves above, adds the one thing genuinely missing (an automated regression test — no test currently exercises `showUpgradeBanner` / `isPremium` footer rendering), and produces a SUMMARY.md so Phase 4 tracking reflects reality. No production code changes are expected; if verification finds a real gap, fix it in `entrypoints/popup/App.tsx` directly.

Purpose: Close the gap between planning artifacts and shipped code without making the executor redo finished work, and add the missing automated test (Nyquist rule — `04-03-PLAN.md` previously had no test file at all for this UI).

Output: `tests/popup-freemium.test.tsx` (new) exercising the banner/badge states. `entrypoints/popup/App.tsx` unchanged unless verification finds a defect.
</objective>

<execution_context>
@$HOME/Documents/Projects - Claude Main Folder/CLI/get-shit-done/workflows/execute-plan.md
@$HOME/Documents/Projects - Claude Main Folder/CLI/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-freemium-store/04-UI-SPEC.md
@.planning/phases/04-freemium-store/04-CONTEXT.md

<interfaces>
<!-- ACTUAL current entrypoints/popup/App.tsx structure (verified by reading the file — this is what exists, not what the original plan assumed) -->

// PopupScreen (lines ~71-169) — receives isPremium as a prop from root App state:
interface PopupScreenProps {
  enabled: boolean;
  count: number;
  isPremium: boolean;
  onToggle: () => void;
  setScreen: (s: Screen) => void;
}

// Banner visibility derivation, inline inside PopupScreen body:
const showUpgradeBanner = !isPremium && count >= 5;

// Banner JSX (already implemented, lines ~95-105):
{showUpgradeBanner && (
  <div className="mx-3 mt-3 rounded-lg border border-[#FDE68A] bg-[#FFFBEB] px-3 py-2 flex items-center justify-between">
    <span className="text-xs font-semibold text-amber-800">Monthly limit reached</span>
    <button onClick={() => chrome.tabs.create({ url: UPGRADE_URL })} className="text-xs font-bold text-amber-700 underline ml-2">
      Upgrade to Premium
    </button>
  </div>
)}

// Footer badge JSX (already implemented, lines ~149-166):
{isPremium ? (
  <span className="text-xs font-bold text-accent">PREMIUM ✓</span>
) : (
  <div className="flex items-center gap-2">
    <span className="bg-surface border border-border text-text-secondary text-xs font-bold px-2 py-0.5 rounded-full">FREE</span>
    <button onClick={() => chrome.tabs.create({ url: UPGRADE_URL })} className="text-xs font-bold text-accent">Upgrade →</button>
  </div>
)}

// Root App component (lines ~576-751) — isPremium derivation (already implemented, lines ~581, ~592-611):
const [isPremium, setIsPremium] = useState(false);
useEffect(() => {
  Promise.all([
    storageEnabled.getValue(),
    storageMonthlyCount.getValue(),
    storageLocalLicenseKey.getValue(),
    storageRules.getValue(),
    storageCustomRules.getValue(),
    storageConflict.getValue(),
  ]).then(([enabledVal, countVal, licenseKey, rulesVal, customRulesVal, conflictVal]) => {
    setIsPremium(!!licenseKey);
    // ... other setters
  }).catch(() => { setLoaded(true); });
}, []);

<!-- From src/lib/constants.ts (already centralized — no per-file UPGRADE_URL duplication) -->
export const UPGRADE_URL = 'https://example.com/upgrade'; // TBD — replace before submission
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify popup freemium UI against must-haves and write regression tests</name>
  <files>entrypoints/popup/App.tsx, tests/popup-freemium.test.tsx</files>
  <read_first>entrypoints/popup/App.tsx, src/lib/storage.ts, src/lib/constants.ts, .planning/phases/04-freemium-store/04-UI-SPEC.md, tests/background.test.ts</read_first>
  <action>
    Read entrypoints/popup/App.tsx in full (751 lines) to confirm the PopupScreen component, the `showUpgradeBanner` derivation, and the footer badge JSX match the `<interfaces>` block above exactly (they should — this was already built and is presented here for the executor's reference, not as new instructions to type in).

    Verify each must-have truth by inspection:
    1. `showUpgradeBanner = !isPremium && count >= 5` exists and gates the amber banner div.
    2. Banner uses `bg-[#FFFBEB]` / `border-[#FDE68A]` / `text-amber-800` — these are the UI-SPEC amber tokens (hex literals matching `amber-50`/`amber-200`, used directly because Tailwind's arbitrary-value syntax was chosen over the named utility class; functionally equivalent — do not "fix" this to `bg-amber-50` unless it changes rendered output, since `#FFFBEB` IS the amber-50 hex value).
    3. Footer badge renders `PREMIUM ✓` when isPremium, `FREE` + `Upgrade →` when not.
    4. Banner button and footer `Upgrade →` button both call `chrome.tabs.create({ url: UPGRADE_URL })`.
    5. `isPremium` is set from `storageLocalLicenseKey.getValue()` inside the mount `useEffect`'s `Promise.all`.

    If any of these 5 checks fails against the actual file content, fix `entrypoints/popup/App.tsx` directly to match the UI-SPEC contract (D-08, D-09, D-10) before proceeding — do not silently document a deviation as "expected."

    Then create `tests/popup-freemium.test.tsx` using `@testing-library/react` (already a devDependency — confirm via `cat package.json` if uncertain) and `fakeBrowser` from `@webext-core/fake-browser` (same pattern as `tests/background.test.ts`). Cover:
    - Render with `storageLocalLicenseKey` unset and `storageMonthlyCount` set to 5 → banner is present in the DOM, footer shows FREE badge.
    - Render with `storageLocalLicenseKey` set to a non-empty string and `storageMonthlyCount` set to 5 → banner is absent, footer shows `PREMIUM ✓`.
    - Render with `storageLocalLicenseKey` unset and `storageMonthlyCount` set to 2 (under limit) → banner is absent.
    - Clicking the banner's "Upgrade to Premium" button calls a stubbed `chrome.tabs.create` with `{ url: UPGRADE_URL }` (stub `chrome.tabs.create` via `vi.fn()` assigned onto the global `chrome.tabs` object — `fakeBrowser` may not implement `tabs.create`; check and stub manually if needed).

    Use `fakeBrowser.storage.local.set({ ... })` with the un-prefixed keys (`monthlyCount`, `licenseKey`, `enabled`, etc. — `@wxt-dev/storage` strips the `local:` prefix, same pattern visible in `tests/background.test.ts` line 26-29).
  </action>
  <verify>
    <automated>cd "C:\Users\maccl\Documents\Projects - Claude Main Folder\Download-Renamer-Web-Extension" && npx vitest run tests/popup-freemium.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>
    - entrypoints/popup/App.tsx confirmed (or fixed) to match all 5 must-have truths
    - tests/popup-freemium.test.tsx exists with at least 4 test cases covering banner visibility, badge state, and upgrade click behavior
    - npx vitest run tests/popup-freemium.test.tsx passes with 0 failures
    - npx wxt build still exits 0 (no regressions introduced)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Popup render → storageLocalLicenseKey | Storage read crosses popup/SW boundary; value comes from chrome.storage.local |
| Popup banner/footer button → chrome.tabs.create | Opens new tab to UPGRADE_URL; URL is hardcoded in extension, not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-11 | Tampering | isPremium in popup | accept | Popup is display-only; gate enforcement is in background.ts (bypassing popup isPremium does not bypass the rename gate) |
| T-04-12 | Spoofing | UPGRADE_URL | accept | URL is hardcoded in extension bundle (src/lib/constants.ts) — no user input involved |
| T-04-SC | Tampering | npm/yarn installs | accept | No new packages; @testing-library/react and @webext-core/fake-browser already present from prior phases |
</threat_model>

<verification>
- entrypoints/popup/App.tsx grep for 'storageLocalLicenseKey' returns ≥1 hit
- entrypoints/popup/App.tsx grep for 'showUpgradeBanner' returns ≥1 hit
- entrypoints/popup/App.tsx grep for 'PREMIUM ✓' returns ≥1 hit
- tests/popup-freemium.test.tsx exists and passes
- wxt build exits 0
</verification>

<success_criteria>
- Popup freemium UI (banner, badge, isPremium wiring) verified to match 04-UI-SPEC.md D-08/D-09/D-10
- Regression test suite exists for the previously-untested popup freemium states
- No outstanding gap between PLAN.md and shipped entrypoints/popup/App.tsx
</success_criteria>

<output>
Create .planning/phases/04-freemium-store/04-03-SUMMARY.md when done
</output>
