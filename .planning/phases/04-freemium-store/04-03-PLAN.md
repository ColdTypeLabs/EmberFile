---
phase: 04-freemium-store
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - entrypoints/popup/App.tsx
autonomous: true
requirements: [MON-02]
user_setup: []

must_haves:
  truths:
    - "When storageLocalLicenseKey is null and monthlyCount >= 5, an amber upgrade banner renders below RenameCountLabel in the popup"
    - "When storageLocalLicenseKey is a non-empty string (premium), the upgrade banner is not rendered"
    - "RenameCountLabel shows '5/5 files this month' when count >= 5 and !isPremium; shows '{N} files renamed this month' otherwise (including when premium and count >= 5)"
    - "Upgrade banner 'Upgrade to Premium' button opens chrome.tabs.create({ url: UPGRADE_URL }) when clicked — does not navigate popup"
    - "isPremium state is derived from storageLocalLicenseKey at popup mount time (useEffect with getValue)"
  artifacts:
    - path: "entrypoints/popup/App.tsx"
      provides: "isPremium wiring + at-limit count format + UpgradeBanner component"
      contains: "isPremium"
  key_links:
    - from: "entrypoints/popup/App.tsx"
      to: "src/lib/storage.ts"
      via: "import storageLocalLicenseKey"
      pattern: "storageLocalLicenseKey"
    - from: "entrypoints/popup/App.tsx"
      to: "04-UI-SPEC.md#upgrade-banner"
      via: "bg-amber-50 border border-amber-200"
      pattern: "amber"
---

<objective>
Wire `isPremium` from storage into the popup and add the Phase 4 UI surfaces: the at-limit count label format and the amber upgrade banner.

Purpose: The popup is the user's first-seen surface when they hit the freemium limit. The banner creates visual contrast (amber vs. white) that draws attention to the upgrade CTA without disrupting the normal-state layout.

Output: entrypoints/popup/App.tsx updated with isPremium state wiring, RenameCountLabel at-limit format, and inline UpgradeBanner component. All copy and visual tokens per 04-UI-SPEC.md.
</objective>

<execution_context>
@$HOME/Documents/Projects - Claude Main Folder/CLI/get-shit-done/workflows/execute-plan.md
@$HOME/Documents/Projects - Claude Main Folder/CLI/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-freemium-store/04-UI-SPEC.md
@.planning/phases/04-freemium-store/04-CONTEXT.md

<interfaces>
<!-- From entrypoints/popup/App.tsx — existing structure (pre-Phase 4) -->
const isPremium = false; // line ~137 — replace this with useState+useEffect

// Popup shell:
<div className="w-[360px] bg-white p-4 flex flex-col gap-3">
  <PopupHeader />
  <PauseResumeButton enabled={state.enabled} onToggle={handleToggle} />
  <RenameCountLabel count={state.count} />
  <div className="flex items-center justify-between">
    <AccountBadge isPremium={isPremium} />
    <ManageRulesLink />
  </div>
</div>

// RenameCountLabel component (existing — reads count prop):
// Currently renders: "{count} files renamed this month"
// Phase 4: conditional format when at limit

// From src/lib/storage.ts (added in 04-01):
export const storageLocalLicenseKey = storage.defineItem<string | null>('local:licenseKey', { fallback: null });
// storageMonthlyCount already exists

<!-- From 04-UI-SPEC.md — UpgradeBanner spec -->
// bg-amber-50 border border-amber-200 rounded px-3 py-2 flex items-center justify-between
// Left: <span className="text-amber-800 text-xs">Monthly limit reached</span>
// Right: <button className="text-amber-800 text-xs font-semibold underline">Upgrade to Premium</button>
// Visibility: {!isPremium && count >= 5 && (...)}

<!-- From 04-UI-SPEC.md — Count label copy -->
// Normal:   "{N} files renamed this month"
// At limit: "5/5 files this month"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire isPremium + add UpgradeBanner + update RenameCountLabel in popup/App.tsx</name>
  <files>entrypoints/popup/App.tsx</files>
  <read_first>entrypoints/popup/App.tsx, src/lib/storage.ts, .planning/phases/04-freemium-store/04-UI-SPEC.md</read_first>
  <action>
    Read entrypoints/popup/App.tsx fully before editing.

    1. Add import for storageLocalLicenseKey at the top of the file, alongside the existing storage imports.

    2. Replace `const isPremium = false;` with:
       ```tsx
       const [isPremium, setIsPremium] = useState(false);
       useEffect(() => {
         storageLocalLicenseKey.getValue().then(key => setIsPremium(!!key));
       }, []);
       ```
       The useEffect has no dependencies beyond initial mount — key redemption only takes effect on popup reopen.

    3. Update RenameCountLabel to support at-limit format. Two approaches — choose the one that fits the existing component implementation best:
       Option A (pass isPremium + atLimit prop to component): Add `isAtLimit={!isPremium && state.count >= 5}` prop; inside the component, render `"5/5 files this month"` when atLimit, else `"{count} files renamed this month"`.
       Option B (derive display string before render, pass as children/label): Compute `const countLabel = (!isPremium && state.count >= 5) ? '5/5 files this month' : \`${state.count} files renamed this month\`` and pass to component.
       Pick whichever requires fewer changes to the existing component. Do NOT introduce new files — if RenameCountLabel is in the same file, edit it inline.

    4. Add the UpgradeBanner inline below `<RenameCountLabel />` in the JSX. Do NOT create a new file — define it as an inline component or a `const UpgradeBanner` at the top of popup/App.tsx:
       ```tsx
       const UPGRADE_URL = 'https://example.com/upgrade'; // Trevor replaces before submission
       // ...
       {!isPremium && state.count >= 5 && (
         <div className="bg-amber-50 border border-amber-200 rounded px-3 py-2 flex items-center justify-between">
           <span className="text-amber-800 text-xs">Monthly limit reached</span>
           <button
             className="text-amber-800 text-xs font-semibold underline"
             onClick={() => chrome.tabs.create({ url: UPGRADE_URL })}
           >
             Upgrade to Premium
           </button>
         </div>
       )}
       ```

    CRITICAL: The UPGRADE_URL in popup/App.tsx is a separate constant from the one in background.ts — both need to be replaced before submission. The STORE-LISTING.md pre-submission checklist covers this (see 04-05).

    Copy must match 04-UI-SPEC.md exactly:
    - Banner left: "Monthly limit reached"
    - Banner CTA: "Upgrade to Premium"
    - Count at limit: "5/5 files this month"
    - Count normal: "{N} files renamed this month"

    Do NOT use useState for the banner visibility — it derives from isPremium and state.count, which are already reactive.
  </action>
  <verify>
    <automated>cd "C:\Users\maccl\Documents\Projects - Claude Main Folder\Download-Renamer-Web-Extension" && npx wxt build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - entrypoints/popup/App.tsx imports storageLocalLicenseKey
    - isPremium is useState(false) + useEffect getValue()
    - RenameCountLabel (or equivalent) renders "5/5 files this month" when !isPremium && count >= 5
    - UpgradeBanner renders below RenameCountLabel when !isPremium && count >= 5
    - Banner uses bg-amber-50, border-amber-200, text-amber-800 tokens
    - Banner CTA click calls chrome.tabs.create({ url: UPGRADE_URL })
    - UPGRADE_URL const declared as placeholder
    - wxt build exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Popup render → storageLocalLicenseKey | Storage read crosses popup/SW boundary; value comes from chrome.storage.local |
| Popup banner button → chrome.tabs.create | Opens new tab to UPGRADE_URL; URL is hardcoded in extension, not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | isPremium in popup | accept | Popup is display-only; gate enforcement is in background.ts (bypassing popup isPremium does not bypass the rename gate) |
| T-04-12 | Spoofing | UPGRADE_URL | accept | URL is hardcoded in extension bundle — no user input involved |
| T-04-SC | Tampering | npm/yarn installs | accept | No new packages; Tailwind amber tokens already included in Phase 3 build |
</threat_model>

<verification>
- entrypoints/popup/App.tsx grep for 'storageLocalLicenseKey' returns ≥1 hit
- entrypoints/popup/App.tsx grep for 'isPremium' returns useState and useEffect usages
- entrypoints/popup/App.tsx grep for 'amber' returns banner container className
- entrypoints/popup/App.tsx grep for 'Monthly limit reached' returns ≥1 hit
- entrypoints/popup/App.tsx grep for '5/5 files this month' returns ≥1 hit
- wxt build exits 0
</verification>

<success_criteria>
- isPremium wired from storageLocalLicenseKey in popup
- RenameCountLabel shows "5/5 files this month" when !isPremium && count >= 5
- Amber upgrade banner renders when !isPremium && count >= 5; hidden when premium
- Banner "Upgrade to Premium" click opens UPGRADE_URL tab
- wxt build passes
</success_criteria>

<output>
Create .planning/phases/04-freemium-store/04-03-SUMMARY.md when done
</output>
