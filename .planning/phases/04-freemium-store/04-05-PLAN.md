---
phase: 04-freemium-store
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/privacy.html
  - STORE-LISTING.md
  - SCREENSHOTS.md
autonomous: false
requirements: [QUAL-03]
user_setup:
  - service: github-pages
    why: "Host privacy policy at a public URL required by Chrome Web Store"
    env_vars: []
    dashboard_config:
      - task: "Enable GitHub Pages: repo Settings → Pages → Source: Deploy from a branch → Branch: main → Folder: /docs → Save"
        location: "GitHub.com — repository Settings → Pages"
      - task: "After enabling, note the Pages URL (e.g. https://ya-boy-mac.github.io/Download-Renamer-Web-Extension/) and update PRIVACY_URL in docs/privacy.html"
        location: "GitHub.com — repository Settings → Pages"

must_haves:
  truths:
    - "docs/privacy.html is a valid HTML page containing all content from PRIVACY.md"
    - "STORE-LISTING.md contains title (≤45 chars), short description (≤132 chars), long description, category 'Productivity', and permission justifications for all 4 permissions"
    - "SCREENSHOTS.md provides numbered step-by-step instructions for capturing each required Chrome Web Store screenshot"
  artifacts:
    - path: "docs/privacy.html"
      provides: "Privacy policy page for GitHub Pages hosting"
      contains: "filenames"
    - path: "STORE-LISTING.md"
      provides: "Store listing copy ready for Chrome Web Store dashboard"
      contains: "Download Renamer"
    - path: "SCREENSHOTS.md"
      provides: "Manual capture guide for required store screenshots"
      contains: "1280"
  key_links:
    - from: "docs/privacy.html"
      to: "GitHub Pages"
      via: "repo Settings → Pages → /docs folder"
      pattern: "privacy\\.html"
---

<objective>
Create all Chrome Web Store submission artifacts: the privacy policy HTML page (for GitHub Pages), the store listing copy document, and the screenshot capture guide.

Purpose: These are the only non-code deliverables in Phase 4. Without them, the extension cannot be submitted regardless of how complete the code is.

Output: docs/privacy.html (new), STORE-LISTING.md (new), SCREENSHOTS.md (new). Trevor enables GitHub Pages via the repository settings checkpoint.
</objective>

<execution_context>
@$HOME/Documents/Projects - Claude Main Folder/CLI/get-shit-done/workflows/execute-plan.md
@$HOME/Documents/Projects - Claude Main Folder/CLI/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-freemium-store/04-CONTEXT.md
@.planning/phases/04-freemium-store/04-RESEARCH.md
@PRIVACY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/privacy.html from PRIVACY.md content</name>
  <files>docs/privacy.html</files>
  <read_first>PRIVACY.md</read_first>
  <action>
    Create the docs/ directory and docs/privacy.html.

    The HTML file must be a complete, standalone page (not a redirect). Structure:
    - `<!DOCTYPE html>` declaration
    - `<html lang="en">` with `<head>` containing: charset UTF-8, viewport meta, title "Download Renamer — Privacy Policy", minimal inline CSS (font-family: sans-serif, max-width: 640px, margin: 2rem auto, padding: 0 1rem, line-height: 1.6)
    - `<body>` with all content from PRIVACY.md converted to semantic HTML (h1, h2, p, ul, li as appropriate)
    - A `<p class="updated">Last updated: [date from PRIVACY.md]</p>` at the top after the h1

    At the bottom of the page, add a comment: `<!-- GitHub Pages URL: update PRIVACY_URL in STORE-LISTING.md once Pages is enabled -->`

    Do NOT use any external fonts, CDN scripts, or remote resources — the page must be self-contained. The Chrome Web Store requires HTTPS and a publicly accessible URL; GitHub Pages provides this.

    Convert all Markdown headings to `<h1>`/`<h2>`, paragraphs to `<p>`, bullet lists to `<ul><li>`, and bold text to `<strong>`. Preserve all content exactly — do not summarize or omit.
  </action>
  <verify>
    <automated>if (Test-Path "C:\Users\maccl\Documents\Projects - Claude Main Folder\Download-Renamer-Web-Extension\docs\privacy.html") { Write-Host "exists" } else { Write-Host "MISSING" }</automated>
  </verify>
  <done>
    - docs/privacy.html exists
    - File contains DOCTYPE, html, head, body tags (valid HTML structure)
    - All PRIVACY.md content is present in HTML form
    - No external scripts or stylesheets (self-contained)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write STORE-LISTING.md with store copy and permission justifications</name>
  <files>STORE-LISTING.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/04-freemium-store/04-RESEARCH.md</read_first>
  <action>
    Create STORE-LISTING.md in the project root. Include these sections:

    ## Extension Name
    Must be ≤45 characters. Suggested: "Download Renamer — AI File Naming" (34 chars). Trevor may edit.

    ## Short Description (≤132 characters)
    Must convey the core value. Suggested: "Auto-renames downloads using AI. First download uses Claude; every repeat uses a saved rule — zero API cost." (105 chars). Trevor may edit.

    ## Long Description
    3-4 paragraphs covering: (1) what it does (AI rename on first encounter, instant local rule on repeats), (2) how it works (intercepts downloads before save, no file contents sent), (3) freemium model (5 free/month, $2.99/mo premium), (4) privacy (only filename sent, not file contents; server logs pattern only). Keep under 1,000 words. Trevor may edit.

    ## Category
    Productivity

    ## Permission Justifications
    Include a section titled "## Permission Justifications (for Chrome Web Store dashboard)" with one bullet per permission:
    - downloads: "Required to intercept file downloads and suggest renamed filenames before the file is written to disk"
    - storage: "Required to persist rename rules, usage counters, and premium status locally on the user's device"
    - alarms: "Required to reset the monthly rename counter on the first day of each calendar month"
    - notifications: "Required to alert users when they reach the monthly free-tier rename limit"

    ## Icon Assets Required (manual — Trevor provides)
    - 16×16 PNG → public/icons/icon-16.png
    - 48×48 PNG → public/icons/icon-48.png
    - 128×128 PNG → public/icons/icon-128.png
    (These are referenced in wxt.config.ts as default WXT icons — confirm location with `npx wxt build` output)

    ## Store Promo Tile
    440×280 PNG — required for store listing (affects ranking). Trevor creates a simple branded image.
    Size: 440px × 280px, PNG format, no animated content.

    ## Privacy Policy URL
    PLACEHOLDER: https://ya-boy-mac.github.io/Download-Renamer-Web-Extension/privacy.html
    → Trevor confirms after enabling GitHub Pages (Settings → Pages → main branch → /docs)

    ## Upgrade URL (replace before submission)
    PLACEHOLDER: https://example.com/upgrade
    → Replace in: entrypoints/background.ts UPGRADE_URL const, entrypoints/options/App.tsx upgrade button onClick, entrypoints/popup/App.tsx upgrade banner button onClick

    ## Pre-Submission Checklist
    - [ ] Icons at public/icons/ (16, 48, 128 px PNGs)
    - [ ] Promo tile 440×280 PNG created
    - [ ] GitHub Pages enabled, privacy.html live and accessible
    - [ ] UPGRADE_URL replaced in background.ts, popup/App.tsx, options/App.tsx
    - [ ] wrangler.toml KV namespace ID filled in (see 04-02 plan)
    - [ ] Worker deployed with real LICENSE_KEYS KV namespace
    - [ ] Extension loads clean in Chrome (no console errors)
    - [ ] Manual test: 6th download shows notification and original filename retained
    - [ ] Manual test: Valid license key activation flips badge to PREMIUM ✓
  </action>
  <verify>
    <automated>if (Test-Path "C:\Users\maccl\Documents\Projects - Claude Main Folder\Download-Renamer-Web-Extension\STORE-LISTING.md") { Write-Host "exists" } else { Write-Host "MISSING" }</automated>
  </verify>
  <done>
    - STORE-LISTING.md exists in project root
    - Contains extension name ≤45 chars
    - Contains short description ≤132 chars
    - Contains long description (3-4 paragraphs)
    - Contains permission justifications for all 4 permissions (downloads, storage, alarms, notifications)
    - Contains pre-submission checklist
    - Privacy policy URL placeholder present
    - Upgrade URL replacement instructions present
  </done>
</task>

<task type="auto">
  <name>Task 3: Write SCREENSHOTS.md with capture instructions</name>
  <files>SCREENSHOTS.md</files>
  <read_first>.planning/phases/04-freemium-store/04-RESEARCH.md</read_first>
  <action>
    Create SCREENSHOTS.md in the project root. Chrome Web Store requires at least 1 screenshot; up to 5 allowed. Preferred size: 1280×800 px (also accepted: 640×400 px). Format: PNG or JPG.

    Provide step-by-step instructions for 4 screenshots:

    ## Screenshot 1: Popup — Normal State
    1. Build and load the extension: `npx wxt build` then load dist/ in Chrome (chrome://extensions → Developer mode → Load unpacked)
    2. Set storageMonthlyCount to 2 via browser console: In Service Worker console (chrome://extensions → Inspect views: service worker): `chrome.storage.local.set({ 'local:monthlyCount': 2 })`
    3. Click the extension icon in Chrome toolbar
    4. Capture the popup showing "2 files renamed this month", the Pause button, and the FREE badge
    5. Crop to exactly the popup area (360px wide). Use the browser's built-in screenshot or a tool like ShareX
    6. Save as: screenshot-1-popup-normal.png (1280×800 ideally, or crop to popup size and center on white background)

    ## Screenshot 2: Options Page — Learned Rules
    1. Add a rule via storage console: `chrome.storage.local.set({ 'local:rules': { 'invoice-pdf': { tag: 'invoice', renameFormat: '{tag}-{date}', matchCount: 3 } } })`
    2. Open options: chrome://extensions → Download Renamer → Details → Extension options (or click "Manage rules →" in popup)
    3. Capture the options page showing the rule row with the pencil and trash icons
    4. Save as: screenshot-2-options-rules.png

    ## Screenshot 3: Popup — Limit Reached (Upgrade Banner)
    1. Set count to 5: `chrome.storage.local.set({ 'local:monthlyCount': 5 })`
    2. Open popup
    3. Capture showing "5/5 files this month" label and the amber upgrade banner below it
    4. Save as: screenshot-3-popup-limit.png

    ## Screenshot 4: Options Page — Key Activation
    1. Open options page
    2. Click "Have a key?" to expand the key input
    3. Type a key string (can be anything for the screenshot — does not need to be valid)
    4. Capture the expanded state showing the input field and Activate button
    5. Save as: screenshot-4-options-key-input.png

    ## Notes
    - Preferred dimensions: 1280×800 px. If the popup/options content is smaller, center on a white 1280×800 canvas.
    - Do NOT include any personal information in screenshots (no real filenames, no email addresses).
    - The promo tile (440×280 PNG) is a separate marketing image — not a screenshot. Create it in Figma, Canva, or similar.
    - Upload screenshots in the Chrome Web Store Developer Dashboard under the "Store listing" tab.
  </action>
  <verify>
    <automated>if (Test-Path "C:\Users\maccl\Documents\Projects - Claude Main Folder\Download-Renamer-Web-Extension\SCREENSHOTS.md") { Write-Host "exists" } else { Write-Host "MISSING" }</automated>
  </verify>
  <done>
    - SCREENSHOTS.md exists in project root
    - Contains instructions for at least 4 screenshots
    - Each screenshot section includes step-by-step capture steps
    - Notes section covers dimension requirements and promo tile distinction
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Enable GitHub Pages and confirm privacy policy URL</name>
  <what-built>docs/privacy.html is created and committed. STORE-LISTING.md has a placeholder Pages URL.</what-built>
  <how-to-verify>
    Step 1 — Push the docs/ directory to the main branch on GitHub (if not already pushed via git push).

    Step 2 — Enable GitHub Pages:
    Go to your repository on GitHub.com → Settings → Pages
    Under "Source": select "Deploy from a branch"
    Branch: main, Folder: /docs
    Click Save

    Step 3 — Wait 1-2 minutes for Pages to build, then confirm the URL:
    The URL will be shown in Settings → Pages (e.g. https://ya-boy-mac.github.io/Download-Renamer-Web-Extension/)
    The privacy policy page URL will be: {base-url}privacy.html

    Step 4 — Verify the page is live:
    Open https://{your-pages-url}/privacy.html in a browser
    Confirm it loads and shows the privacy policy content

    Step 5 — Update STORE-LISTING.md:
    Replace the placeholder privacy policy URL (currently "https://ya-boy-mac.github.io/...") with the confirmed live URL from Step 3.
  </how-to-verify>
  <resume-signal>Type "pages-live" and paste the confirmed privacy policy URL, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages → Chrome Web Store | Public HTML page; no user data processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | docs/privacy.html | accept | Static public page by design; contains only policy text, no user data |
| T-04-10 | Tampering | STORE-LISTING.md content | accept | Internal document; Chrome Web Store reviews content before publishing |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
- docs/privacy.html exists and contains valid HTML structure (DOCTYPE, html, head, body)
- docs/privacy.html contains all content from PRIVACY.md (no content omitted)
- STORE-LISTING.md exists and title field is ≤45 characters
- STORE-LISTING.md exists and short description is ≤132 characters
- STORE-LISTING.md contains justifications for all 4 permissions (downloads, storage, alarms, notifications)
- SCREENSHOTS.md exists with instructions for ≥4 screenshots
- After checkpoint: privacy.html live at GitHub Pages URL
</verification>

<success_criteria>
- docs/privacy.html created from PRIVACY.md with valid HTML (no external resources)
- STORE-LISTING.md created with title, short desc, long desc, permission justifications, pre-submission checklist
- SCREENSHOTS.md created with step-by-step capture instructions for 4 screenshots
- GitHub Pages enabled; privacy.html accessible at public URL (after checkpoint)
- STORE-LISTING.md updated with confirmed live privacy policy URL
</success_criteria>

<output>
Create .planning/phases/04-freemium-store/04-05-SUMMARY.md when done
</output>
