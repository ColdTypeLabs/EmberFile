---
phase: 04-freemium-store
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - entrypoints/popup/App.tsx
  - tests/popup-key-redemption.test.tsx
autonomous: true
requirements: [MON-02, MON-03]

must_haves:
  truths:
    - "In SettingsScreen, isPremium is derived from storageLocalLicenseKey via the root App component's mount-time useEffect (no separate options-page state — options/App.tsx is now a stub directing users to the popup)"
    - "When !isPremium, a 'Have a key?' link renders below the 'Upgrade to Premium' button inside the Your Account card in SettingsScreen"
    - "Clicking 'Have a key?' reveals an inline key input + 'Activate' button; the link itself disappears"
    - "Clicking 'Activate' with a non-empty key shows 'Activating…', POSTs to `${VITE_WORKER_URL}/validate-key`, on success writes the key to storageLocalLicenseKey and flips isPremium to true with no page reload, on invalid-key response shows 'Invalid key — please check and try again.', on network/timeout failure shows 'Activation failed — check your connection and try again.'"
    - "Clicking 'Activate' with an empty/whitespace-only key does nothing — the button is disabled"
    - "On successful activation, the key input row collapses and the AccountBadge in both SettingsScreen and PopupScreen reflects PREMIUM ✓ immediately (shared isPremium state, no remount needed)"
  artifacts:
    - path: "entrypoints/popup/App.tsx"
      provides: "Key redemption UX in SettingsScreen's Your Account card, already implemented"
      contains: "handleActivateKey"
    - path: "tests/popup-key-redemption.test.tsx"
      provides: "Regression coverage for the Activate flow: success, invalid key, network error, empty-input guard"
      contains: "handleActivateKey"
  key_links:
    - from: "entrypoints/popup/App.tsx"
      to: "src/lib/storage.ts"
      via: "storageLocalLicenseKey.setValue(keyValue.trim())"
      pattern: "storageLocalLicenseKey\\.setValue"
    - from: "entrypoints/popup/App.tsx"
      to: "workers/rename-relay/index.ts"
      via: "fetch(`${VITE_WORKER_URL}/validate-key`"
      pattern: "validate-key"
---

<objective>
Reconciliation plan — NOT new implementation. The original 04-04-PLAN.md targeted `entrypoints/options/App.tsx` with an `AccountSection` component. That architecture was superseded by an ad-hoc popup redesign (see STATE.md, 04-01-SUMMARY.md context, and the orchestrator's reality check): `entrypoints/options/App.tsx` is now a 9-line stub ("Settings are now in the extension popup"), and the full key redemption UX — `showKeyInput`, `keyValue`, `keyActivating`, `keyError`, `handleActivateKey` — lives inside `entrypoints/popup/App.tsx`'s `SettingsScreen` component and root `App` component (lines ~192-330 for SettingsScreen props/JSX, ~587-647 for state and handleActivateKey).

This plan verifies the already-built key redemption flow against the goal-backward must-haves above, adds the missing automated test coverage (no test currently exercises `handleActivateKey`), and produces a SUMMARY.md so Phase 4 tracking reflects reality. No production code changes are expected unless verification finds a real defect.

The cross-context staleness concern raised in the Codex review (MEDIUM: "popup isPremium staleness... since popup/options are separate extension contexts") is now LARGELY MOOT: there is only one extension UI surface (the popup, which now contains all three screens), so there is no separate options-page state to go stale relative to the popup. Note this explicitly in the SUMMARY rather than treating it as still-open.

Purpose: Close the gap between planning artifacts and shipped code, add the missing automated test (Nyquist rule — original 04-04-PLAN.md had no test file), and capture the architecture-divergence rationale in writing.

Output: `tests/popup-key-redemption.test.tsx` (new). `entrypoints/popup/App.tsx` unchanged unless verification finds a defect.
</objective>

<execution_context>
@$HOME/Documents/Projects - Claude Main Folder/CLI/get-shit-done/workflows/execute-plan.md
@$HOME/Documents/Projects - Claude Main Folder/CLI/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-freemium-store/04-UI-SPEC.md
@.planning/phases/04-freemium-store/04-CONTEXT.md
@.planning/phases/04-freemium-store/04-RESEARCH.md

<interfaces>
<!-- ACTUAL current entrypoints/popup/App.tsx structure (verified by reading the file) -->

// Root App component state (lines ~576-590):
const [isPremium, setIsPremium] = useState(false);
const [showKeyInput, setShowKeyInput] = useState(false);
const [keyValue, setKeyValue] = useState('');
const [keyActivating, setKeyActivating] = useState(false);
const [keyError, setKeyError] = useState<string | null>(null);

// handleActivateKey (root App component, lines ~623-647):
async function handleActivateKey() {
  setKeyActivating(true);
  setKeyError(null);
  try {
    const res = await fetch(`${import.meta.env.VITE_WORKER_URL}/validate-key`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ key: keyValue.trim() }),
    });
    const data = await res.json();
    if (!data.valid) throw new Error('invalid');
    await storageLocalLicenseKey.setValue(keyValue.trim());
    setIsPremium(true);
    setShowKeyInput(false);
    setKeyValue('');
  } catch (e) {
    setKeyError(
      e instanceof TypeError
        ? 'Activation failed — check your connection and try again.'
        : 'Invalid key — please check and try again.'
    );
  } finally {
    setKeyActivating(false);
  }
}

// SettingsScreen JSX — "Have a key?" link + expanded input row (lines ~298-326), inside the
// "Your Account" card, below the "Upgrade to Premium" button:
{!isPremium && !showKeyInput && (
  <button onClick={() => setShowKeyInput(true)} className="text-xs text-text-muted underline text-left mt-1">
    Have a key?
  </button>
)}
{!isPremium && showKeyInput && (
  <div className="flex flex-col gap-1 mt-1">
    <input type="text" value={keyValue} onChange={(e) => setKeyValue(e.target.value)} placeholder="Enter your license key" className="..." />
    <button disabled={keyActivating || !keyValue.trim()} onClick={onActivateKey} className="...">
      {keyActivating ? 'Activating…' : 'Activate'}
    </button>
    {keyError && <p className="text-xs text-red-400 mt-1">{keyError}</p>}
  </div>
)}

<!-- From src/lib/storage.ts (added in 04-01) -->
export const storageLocalLicenseKey = storage.defineItem<string | null>('local:licenseKey', { fallback: null });

<!-- From workers/rename-relay/index.ts (added in 04-02, response-code normalization confirmed correct) -->
// POST /validate-key — body: { key: string } — returns: 400 { valid: false } (empty/missing key),
// 200 { valid: true|false } (KV lookup either way), 500 { valid: false } (unexpected error)

<!-- Worker URL pattern (existing, same env var the rename relay call already uses in this file) -->
const WORKER_URL = import.meta.env.VITE_WORKER_URL;

<!-- From 04-UI-SPEC.md — copy contract (locked, do not paraphrase) -->
// "Have a key?" expand trigger
// Input placeholder: "Enter your license key"
// Activate button: "Activate" / loading: "Activating…"
// Error (invalid): "Invalid key — please check and try again."
// Error (network): "Activation failed — check your connection and try again."
// Success: key input collapses, badge flips to PREMIUM ✓ (no toast)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify popup key redemption flow against must-haves and write regression tests</name>
  <files>entrypoints/popup/App.tsx, tests/popup-key-redemption.test.tsx</files>
  <read_first>entrypoints/popup/App.tsx, src/lib/storage.ts, workers/rename-relay/index.ts, .planning/phases/04-freemium-store/04-UI-SPEC.md, tests/background.test.ts</read_first>
  <action>
    Read entrypoints/popup/App.tsx in full to confirm `handleActivateKey`, the SettingsScreen key-redemption JSX, and the state wiring match the `<interfaces>` block above exactly (already built — presented for the executor's reference).

    Verify each must-have truth by inspection:
    1. `handleActivateKey` POSTs to `${import.meta.env.VITE_WORKER_URL}/validate-key` with `{ key: keyValue.trim() }`.
    2. On `data.valid === true`: `storageLocalLicenseKey.setValue(keyValue.trim())` is awaited, `setIsPremium(true)`, `setShowKeyInput(false)`, `setKeyValue('')` all fire — no page reload anywhere in the flow (single-page React app, state update is sufficient).
    3. On `data.valid === false`: catch block sets `'Invalid key — please check and try again.'` (verify the `e instanceof TypeError` branch correctly distinguishes a thrown `new Error('invalid')` — which is NOT a TypeError — from a genuine network failure — `fetch` rejecting IS a TypeError in browsers. This existing logic is correct; do not "simplify" it).
    4. On network failure (fetch rejects): catch block sets `'Activation failed — check your connection and try again.'`.
    5. The Activate button has `disabled={keyActivating || !keyValue.trim()}` — empty/whitespace input cannot submit.
    6. "Have a key?" only renders when `!isPremium && !showKeyInput`; the input row only renders when `!isPremium && showKeyInput` — mutually exclusive.

    If any check fails against the actual file content, fix `entrypoints/popup/App.tsx` directly before proceeding.

    Note: there is no explicit 5-second `Promise.race` timeout wrapping the `fetch` call in `handleActivateKey` (unlike the original 04-04-PLAN.md draft, which specified one). A hung `fetch` with no timeout will leave `keyActivating` true indefinitely with no user-facing error. This is a real gap relative to the CLAUDE.md "all Worker calls use 5-second Promise.race timeout" pattern (already applied in `entrypoints/background.ts`'s rename relay call). Add the same `Promise.race` timeout pattern to `handleActivateKey`:
    ```
    const res = await Promise.race([
      fetch(`${import.meta.env.VITE_WORKER_URL}/validate-key`, { ... }),
      new Promise<never>((_, reject) => setTimeout(() => reject(new TypeError('timeout')), 5000)),
    ]);
    ```
    Throwing a `TypeError` on timeout keeps the existing `e instanceof TypeError` branch routing the timeout case to the network-error copy ("Activation failed...") rather than the invalid-key copy — this matches user expectation (a timeout is not "your key was wrong").

    Then create `tests/popup-key-redemption.test.tsx` using `@testing-library/react` and `fakeBrowser` (same pattern as `tests/background.test.ts` and `tests/popup-freemium.test.tsx` from 04-03). Cover:
    - Navigate to SettingsScreen, click "Have a key?" → input row appears, link disappears.
    - Type a key, click Activate, mock `fetch` resolving `{ valid: true }` → `storageLocalLicenseKey` is set, `isPremium` becomes true, input collapses, badge shows `PREMIUM ✓`.
    - Type a key, click Activate, mock `fetch` resolving `{ valid: false }` → error text `'Invalid key — please check and try again.'` renders, input stays visible.
    - Type a key, click Activate, mock `fetch` rejecting (simulate `TypeError`) → error text `'Activation failed — check your connection and try again.'` renders.
    - Type a key, click Activate, mock `fetch` hanging past 5 seconds (use `vi.useFakeTimers()` + `vi.advanceTimersByTime(5000)`) → timeout fires, network-error copy renders, `keyActivating` returns to false.
    - Activate button is disabled when the input is empty or whitespace-only.
  </action>
  <verify>
    <automated>cd "C:\Users\maccl\Documents\Projects - Claude Main Folder\Download-Renamer-Web-Extension" && npx vitest run tests/popup-key-redemption.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>
    - entrypoints/popup/App.tsx handleActivateKey confirmed to wrap fetch in a 5-second Promise.race timeout (added if missing)
    - tests/popup-key-redemption.test.tsx exists with at least 6 test cases per the action list above
    - npx vitest run tests/popup-key-redemption.test.tsx passes with 0 failures
    - npx wxt build still exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Popup → Worker /validate-key | User-supplied key string is the only input; extension POSTs to Trevor's Worker |
| Worker response → storageLocalLicenseKey | Valid=true response from Worker triggers write to storage; no response data except the boolean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-13 | Tampering | keyValue XSS | accept | Key is sent as JSON body string to Worker; never rendered as HTML — no XSS risk |
| T-04-14 | Tampering | validate-key response spoofing | accept | User could MITM their own request to get valid=true; accepted in v1 per CONTEXT.md D-15 (same as DevTools bypass acceptance) |
| T-04-15 | Information Disclosure | key string stored in chrome.storage.local | accept | Storage is sandboxed to extension; key is not logged or transmitted after storage |
| T-04-16 | Denial of Service | /validate-key hang with no client timeout | mitigate | This plan adds a 5-second Promise.race timeout to handleActivateKey — closes a real gap found during verification (the fetch call had no timeout, unlike the background.ts Worker call pattern) |
| T-04-SC | Tampering | npm/yarn installs | accept | No new packages; fetch is native browser API; @testing-library/react already present |
</threat_model>

<verification>
- entrypoints/popup/App.tsx grep for 'handleActivateKey' returns ≥1 hit
- entrypoints/popup/App.tsx grep for 'showKeyInput' returns ≥3 hits (state init + two conditional renders)
- entrypoints/popup/App.tsx grep for 'validate-key' returns ≥1 hit in fetch URL
- entrypoints/popup/App.tsx grep for 'Invalid key' returns exact error string match
- entrypoints/popup/App.tsx grep for 'Activation failed' returns exact error string match
- entrypoints/popup/App.tsx grep for 'Have a key?' returns ≥1 hit in JSX button text
- tests/popup-key-redemption.test.tsx exists and passes
- wxt build exits 0
</verification>

<success_criteria>
- Key redemption flow verified to match 04-UI-SPEC.md D-19 through D-22
- 5-second timeout gap closed (real defect found during verification, not present in original plan text)
- Regression test suite exists for the previously-untested Activate flow (success, invalid, network error, timeout, empty-input guard)
- entrypoints/options/App.tsx stub status confirmed intentional — no work needed there
</success_criteria>

<output>
Create .planning/phases/04-freemium-store/04-04-SUMMARY.md when done
</output>
